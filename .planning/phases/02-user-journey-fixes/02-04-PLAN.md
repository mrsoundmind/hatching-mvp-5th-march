---
phase: 02-user-journey-fixes
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - client/src/components/CenterPanel.tsx
autonomous: false
requirements:
  - UX-02
  - UX-04
  - UX-06
  - UX-07
must_haves:
  truths:
    - "UX-04: Clicking a project in the sidebar expands it and auto-collapses all others"
    - "UX-06: The textarea is always enabled — user can type while the AI streams a response"
    - "UX-07: Agent bubble color is the same before and after navigating to a different project and back"
    - "UX-02: First-time user completing onboarding lands in a project with an active Maya agent conversation"
  artifacts:
    - path: "client/src/components/CenterPanel.tsx"
      provides: "activeProjectAgents in agentRole backfill useEffect dependency array"
      contains: "activeProjectAgents"
  key_links:
    - from: "CenterPanel agentRole backfill useEffect"
      to: "activeProjectAgents"
      via: "dependency array includes activeProjectAgents"
      pattern: "activeProjectAgents.*\\]|\\[.*activeProjectAgents"
---

<objective>
Two goals: (1) Fix the remaining stale-closure bug in CenterPanel that causes agent colors to show incorrectly after navigation (UX-07), and (2) manually verify the four requirements that research confirmed are already implemented correctly — UX-04, UX-06, UX-02, and UX-07 visual outcome.

Purpose: UX-07 has a known stale-closure risk that causes old `activeProjectAgents` to be used for the backfill transform when navigating between projects. Adding it to the useEffect dependency array ensures the transform re-runs with the correct agent list. The manual checkpoint confirms that UX-04, UX-06, and UX-02 work as expected in the browser.
Output: CenterPanel useEffect dependency array updated. Manual verification confirms all four requirements pass.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-user-journey-fixes/02-RESEARCH.md
@.planning/phases/02-user-journey-fixes/02-01-SUMMARY.md
@.planning/phases/02-user-journey-fixes/02-02-SUMMARY.md
@.planning/phases/02-user-journey-fixes/02-03-SUMMARY.md

<interfaces>
<!-- From CenterPanel.tsx — agentRole backfill useEffect (around line 1138) -->
```typescript
// The useEffect that transforms API messages and backfills agentRole
// It reads activeProjectAgents to look up role by agentId
// BUG: activeProjectAgents is NOT in the dependency array
// When user navigates to another project, activeProjectAgents changes but
// if apiMessages hasn't changed, the effect doesn't re-run → stale backfill
useEffect(() => {
  // ... transform apiMessages, backfill agentRole using activeProjectAgents
}, [apiMessages /*, activeProjectAgents is missing */]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add activeProjectAgents to agentRole backfill useEffect dependency array</name>
  <files>client/src/components/CenterPanel.tsx</files>
  <read_first>
    - client/src/components/CenterPanel.tsx (search for "activeProjectAgents.find" or "agentRole" near a useEffect — find the message transform effect around line 1138-1188)
  </read_first>
  <action>
In `client/src/components/CenterPanel.tsx`, find the `useEffect` that:
1. Reads `apiMessages` (from the TanStack Query result)
2. Transforms messages and backfills `agentRole` using `activeProjectAgents.find(a => a.id === msg.agentId)?.role`
3. Calls something like `setAllMessages` or updates the local message state

The dependency array of this effect currently includes `apiMessages` (and possibly `conversationId`) but is MISSING `activeProjectAgents`.

Add `activeProjectAgents` to the dependency array of this specific useEffect:

```typescript
// BEFORE:
}, [apiMessages]);
// or
}, [apiMessages, conversationId]);

// AFTER:
}, [apiMessages, activeProjectAgents]);
// or (if conversationId was already present):
}, [apiMessages, conversationId, activeProjectAgents]);
```

IMPORTANT: Read the useEffect carefully before editing. Only add `activeProjectAgents` to the dependency array — do NOT move, reorder, or change any logic inside the effect body. Only touch the array at the closing `}` line of this specific useEffect.

If `activeProjectAgents` is not a prop but is derived inside the component, verify it is in scope at the useEffect location. If it is passed as a prop (which it is — CenterPanel receives `activeProjectAgents` as a prop from home.tsx), it is already in scope.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "activeProjectAgents" client/src/components/CenterPanel.tsx | grep -v "^.*//.*activeProjectAgents"` shows `activeProjectAgents` in a `], [` dependency array line (not just as a variable reference in the body)
    - `npm run typecheck` exits with code 0
  </acceptance_criteria>
  <done>The agentRole backfill useEffect re-runs whenever activeProjectAgents changes (project navigation), ensuring bubble colors are always derived from the correct project's agent list.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all Phase 2 requirements in the browser</name>
  <files>none</files>
  <action>Start dev server with `npm run dev` and manually verify all 9 phase success criteria listed in how-to-verify.</action>
  <verify>
    <automated>MISSING — this task requires browser verification</automated>
  </verify>
  <what-built>
Plans 01-03 implemented:
- `/` route shows LandingPage for logged-out users (Plan 01, Task 1)
- home.tsx modal creates idea projects with Maya (Plan 01, Task 2)
- Team accordion: one team expanded at a time (Plan 02, Task 1)
- Typing indicator dedup: cleared when streaming starts (Plan 02, Task 2)
- Server-side agentRole backfill for old messages (Plan 03)
- activeProjectAgents in backfill useEffect dep array (Plan 04, Task 1)
  </what-built>
  <how-to-verify>
Start the dev server: `npm run dev`

**UX-03 (landing page):**
1. Sign out of Hatchin
2. Visit http://localhost:5001/
3. Expected: LandingPage renders with Hatchin's value proposition
4. Expected: You are NOT redirected to /login

**UX-01 + UX-02 (project creation + onboarding):**
5. Sign in
6. If you have projects: click the "+ New" button in the CenterPanel (empty state) OR the "+ New project" in LeftSidebar
7. Click "Start with an idea" → enter a project name → confirm
8. Expected: egg hatching animation plays
9. Expected: new project appears in sidebar, Maya is ready, you land in the project chat

**UX-04 (project accordion):**
10. If you have multiple projects: click a project in the left sidebar
11. Expected: that project expands, ALL other projects collapse
12. Click the same project again → Expected: it collapses

**UX-05 (team accordion):**
13. Expand a project that has teams
14. Click one team → Expected: agents appear under that team
15. Click a different team → Expected: first team collapses, second team expands

**UX-06 (textarea during streaming):**
16. Send a message to a Hatch
17. While the response is streaming → click the textarea and try typing
18. Expected: textarea accepts input; you can type a follow-up

**UX-07 (color consistency on navigation):**
19. In a project with messages from multiple agents, note the colors of a few agent bubbles
20. Navigate to a different project
21. Navigate back to the first project
22. Expected: agent bubble colors are exactly the same as in step 19 (no reset to default green)

**UX-08 (single typing indicator):**
23. Send a message
24. Watch carefully during the "thinking" phase and the start of streaming
25. Expected: you see AT MOST ONE typing/thinking indicator at any time — never two simultaneously

**DATA-04 (old message colors):**
26. If you have conversations with messages from before Phase 1 (no agentRole in metadata), reload the page
27. Expected: those messages now show role-appropriate colors (not all green)
28. (If all your messages are new/post-Phase-1, verify via browser network tab — messages API response should include `agentRole` in metadata for all agent messages)
  </how-to-verify>
  <done>All 9 phase success criteria from ROADMAP.md verified in the browser.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any specific failure (which UX item, what you saw vs expected)</resume-signal>
</task>

</tasks>

<verification>
After Task 1 (automated):
```bash
cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npm run typecheck && npm run test:integrity && npm run test:dto
```

After Task 2 (manual checkpoint):
All 9 phase success criteria from ROADMAP.md verified in browser:
1. Create Project → project created, selected, chat ready
2. `/` logged out → LandingPage renders
3. `/` logged in → redirect to app
4. Click project in sidebar → expands, others collapse
5. Click team → agents appear; click again → collapse
6. AI streaming → textarea fully enabled, user can type
7. Navigate away and back → agent bubble color is same
8. AI thinking → typing indicator in exactly one place
9. Old messages → correct agent role color (not always green)
</verification>

<success_criteria>
- `activeProjectAgents` in CenterPanel message-transform useEffect dependency array
- Human checkpoint: all 9 ROADMAP success criteria verified in browser
- `npm run typecheck` passes
- `npm run test:integrity` passes
- `npm run test:dto` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-user-journey-fixes/02-04-SUMMARY.md`
</output>
