---
phase: 05-route-architecture-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/teams.ts
  - server/routes/agents.ts
  - server/routes/messages.ts
  - server/routes.ts
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "GET/POST/PUT/PATCH/DELETE /api/teams* are handled by server/routes/teams.ts"
    - "GET/POST/PUT/PATCH/DELETE /api/agents* are handled by server/routes/agents.ts"
    - "GET/POST/PUT/DELETE /api/conversations* and /api/messages* and /api/training/feedback are handled by server/routes/messages.ts"
    - "npm run typecheck passes with zero errors after extraction"
  artifacts:
    - path: "server/routes/teams.ts"
      provides: "All team CRUD routes"
      exports: ["registerTeamRoutes"]
    - path: "server/routes/agents.ts"
      provides: "All agent CRUD routes"
      exports: ["registerAgentRoutes"]
    - path: "server/routes/messages.ts"
      provides: "All conversation, message, reaction, and feedback routes"
      exports: ["registerMessageRoutes"]
  key_links:
    - from: "server/routes.ts"
      to: "server/routes/teams.ts"
      via: "registerTeamRoutes(app) call"
      pattern: "registerTeamRoutes\\(app\\)"
    - from: "server/routes.ts"
      to: "server/routes/agents.ts"
      via: "registerAgentRoutes(app) call"
      pattern: "registerAgentRoutes\\(app\\)"
    - from: "server/routes.ts"
      to: "server/routes/messages.ts"
      via: "registerMessageRoutes(app) call"
      pattern: "registerMessageRoutes\\(app\\)"
---

<objective>
Extract teams, agents, and messages route groups from the 4,347-line `server/routes.ts` god file into self-contained modules following the established `registerXxxRoutes(app)` pattern from `autonomy.ts`.

Purpose: Begin the route modularization (ARCH-01) with the three simplest, fully self-contained route groups that have zero WS/broadcast dependencies.

Output: Three new route modules in `server/routes/`, corresponding blocks deleted from `routes.ts`, typecheck green.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-route-architecture-cleanup/05-RESEARCH.md

<interfaces>
<!-- Pattern to follow: verified from server/routes/autonomy.ts -->
<!-- Each module re-declares helpers locally; imports only from ../storage.js and @shared/* -->

From server/routes/autonomy.ts (the template):
```typescript
import type { Express, Request } from 'express';
import { storage } from '../storage.js';

export function registerAutonomyRoutes(app: Express): void {
  const getSessionUserId = (req: Request): string | undefined => (req.session as any)?.userId as string | undefined;
  const getOwnedProjectIds = async (userId: string): Promise<Set<string>> => {
    const projects = await storage.getProjects();
    return new Set(projects.filter((project: any) => project.userId === userId).map((project) => project.id));
  };
  const requireOwnedProject = async (projectId: string, userId: string) => {
    const project = await storage.getProject(projectId);
    if (!project) return null;
    return (project as any).userId === userId ? project : null;
  };
  // ... routes registered on app
}
```

Shared helpers that MUST be re-declared locally in each new module (NOT imported from routes.ts):
- `getSessionUserId(req)` — 1-liner: `(req.session as any).userId as string`
- `getOwnedProjectIds(userId)` — async, filters storage.getProjects()
- `getOwnedProject(projectId, userId)` — async, single project ownership check
- `getOwnedTeam(teamId, userId)` — async, checks team ownership via project (needed by teams.ts)
- `getOwnedAgent(agentId, userId)` — async, checks agent ownership via project (needed by agents.ts)
- `conversationOwnedByUser(conversationId, userId)` — async, checks conversation ownership (needed by messages.ts)
- `devLog(...args)` — local console guard: `if (process.env.NODE_ENV !== 'production') console.log(...args)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract teams.ts and agents.ts route modules</name>
  <files>server/routes/teams.ts, server/routes/agents.ts, server/routes.ts</files>
  <read_first>
    - server/routes.ts (lines 1-15 for devLog, lines 327-352 for helpers, lines 566-672 for teams, lines 674-797 for agents)
    - server/routes/autonomy.ts (lines 1-44 for the pattern to follow)
    - shared/schema.ts (for insertTeamSchema, insertAgentSchema types)
  </read_first>
  <action>
    **Create server/routes/teams.ts** (~130 lines):
    1. Import `type { Express, Request } from 'express'`, `{ storage } from '../storage.js'`, `{ insertTeamSchema } from '@shared/schema'`, `{ buildConversationId } from '@shared/conversationId'`, `{ z } from 'zod'`.
    2. Export `function registerTeamRoutes(app: Express): void`.
    3. Inside the function, re-declare locally: `devLog`, `getSessionUserId`, `getOwnedProjectIds`, `getOwnedProject`, `getOwnedTeam`.
    4. Move the exact route handlers from routes.ts lines 566-672 into the function body. These are:
       - `GET /api/teams` (line 567)
       - `GET /api/projects/:projectId/teams` (line 577)
       - `POST /api/teams` (line 590) — includes auto-create team conversation using `buildConversationId`
       - `DELETE /api/teams/:id` (line 625)
       - `PUT /api/teams/:id` (line 641)
       - `PATCH /api/teams/:id` (line 658)
    5. Zero logic changes — move the handler bodies exactly as they are.

    **Create server/routes/agents.ts** (~150 lines):
    1. Import `type { Express, Request } from 'express'`, `{ storage } from '../storage.js'`, `{ insertAgentSchema } from '@shared/schema'`, `{ buildConversationId } from '@shared/conversationId'`, `{ z } from 'zod'`.
    2. Export `function registerAgentRoutes(app: Express): void`.
    3. Inside the function, re-declare locally: `devLog`, `getSessionUserId`, `getOwnedProjectIds`, `getOwnedProject`, `getOwnedAgent`.
    4. Move the exact route handlers from routes.ts lines 674-797. These are:
       - `GET /api/agents` (line 675)
       - `GET /api/projects/:projectId/agents` (line 685)
       - `GET /api/teams/:teamId/agents` (line 696)
       - `POST /api/agents` (line 707) — includes auto-create agent conversation using `buildConversationId`
       - `DELETE /api/agents/:id` (line 745)
       - `PUT /api/agents/:id` (line 763)
       - `PATCH /api/agents/:id` (line 780)
    5. Zero logic changes — move the handler bodies exactly as they are.

    **Update server/routes.ts:**
    1. Add imports at top: `import { registerTeamRoutes } from './routes/teams.js'` and `import { registerAgentRoutes } from './routes/agents.js'`.
    2. Delete lines 566-672 (teams block) and 674-797 (agents block) from routes.ts.
    3. Add `registerTeamRoutes(app);` and `registerAgentRoutes(app);` calls inside `registerRoutes()`, placed right after the helper declarations (around line 364, where the deleted blocks used to be). The exact position does not matter because Express registers routes in the order they're called, and these don't conflict with any other routes.
    4. Remove any now-unused imports from routes.ts top (e.g., if `insertTeamSchema` or `insertAgentSchema` are no longer used in routes.ts — check carefully since `insertAgentSchema` and `insertTeamSchema` may not appear elsewhere in routes.ts).

    CRITICAL: Do NOT import from '../routes.js' in the new modules. Re-declare helpers locally.
    CRITICAL: Use `.js` extension on all local imports (e.g., `'../storage.js'`, not `'../storage'`).
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `server/routes/teams.ts` exists and exports `registerTeamRoutes`
    - `server/routes/agents.ts` exists and exports `registerAgentRoutes`
    - grep for `app.get("/api/teams"` in server/routes.ts returns ZERO matches
    - grep for `app.get("/api/agents"` in server/routes.ts returns ZERO matches
    - grep for `registerTeamRoutes(app)` in server/routes.ts returns exactly 1 match
    - grep for `registerAgentRoutes(app)` in server/routes.ts returns exactly 1 match
    - `npm run typecheck` exits with code 0
  </acceptance_criteria>
  <done>Teams and agents routes live in their own modules. routes.ts no longer contains any /api/teams or /api/agents handlers. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Extract messages.ts route module (conversations + messages + reactions + feedback)</name>
  <files>server/routes/messages.ts, server/routes.ts</files>
  <read_first>
    - server/routes.ts (lines 799-1015 for conversations/messages, lines 1046-1169 for reactions/feedback, lines 354-363 for conversationOwnedByUser helper)
    - server/routes/autonomy.ts (lines 1-44 for the pattern)
    - shared/schema.ts (for insertMessageSchema, insertConversationSchema)
  </read_first>
  <action>
    **Create server/routes/messages.ts** (~420 lines):
    1. Imports needed:
       ```typescript
       import type { Express, Request } from 'express';
       import { storage } from '../storage.js';
       import { insertMessageSchema, insertConversationSchema } from '@shared/schema';
       import { z } from 'zod';
       import { personalityEngine } from '../ai/personalityEvolution.js';
       import { trainingSystem } from '../ai/trainingSystem.js';
       import { markSkillUsed } from '../knowledge/skillUpdates/skillLearner.js';
       ```
    2. Export `function registerMessageRoutes(app: Express): void`.
    3. Inside the function, re-declare locally: `devLog`, `getSessionUserId`, `getOwnedProjectIds`, `getOwnedProject`, `conversationOwnedByUser`.
    4. Move these route handlers from routes.ts:
       - **Conversations** (lines 799-1015):
         - `GET /api/conversations/:projectId` (line 800)
         - `POST /api/conversations` (line 813)
         - `GET /api/conversations/:conversationId/messages` (line 832) — includes agentRole backfill + pagination envelope
         - `POST /api/conversations/:conversationId/messages` (line 896)
         - `POST /api/messages` (line 929) — bulk message creation
         - `PUT /api/conversations/:conversationId/archive` (line 956)
         - `PUT /api/conversations/:conversationId/unarchive` (line 972)
         - `GET /api/projects/:projectId/conversations/archived` (line 988)
         - `DELETE /api/conversations/:id` (line 1001)
       - **Reactions** (lines 1046-1137):
         - `POST /api/messages/:messageId/reactions` (line 1047) — includes personality evolution + skill learner integration
         - `GET /api/messages/:messageId/reactions` (line 1140)
       - **Feedback** (lines 1151-1169):
         - `POST /api/training/feedback` (line 1152)
    5. Zero logic changes. Move handler bodies exactly as-is.

    **Update server/routes.ts:**
    1. Add import: `import { registerMessageRoutes } from './routes/messages.js'`.
    2. Delete lines 799-1015 (conversations/messages block) and lines 1046-1169 (reactions/feedback block) from routes.ts.
    3. Add `registerMessageRoutes(app);` call inside `registerRoutes()`.
    4. Remove now-unused imports from routes.ts top:
       - `insertMessageSchema` (only if no longer used in routes.ts — check if WS handler uses it)
       - `insertConversationSchema` (only if no longer used elsewhere in routes.ts)
       - `personalityEngine` (only if no longer used in routes.ts streaming handler — it IS used in the streaming handler, so KEEP this import)
       - `trainingSystem` (only if no longer used — likely only used in the moved feedback route, so remove)
       - `markSkillUsed` (only if no longer used — likely only used in the moved reaction route, so remove)
    5. IMPORTANT: Do NOT remove `/api/hatch/chat` route (line 1017-1044). That stays in routes.ts for now.

    CRITICAL: The `conversationOwnedByUser` helper must be re-declared locally in messages.ts. It depends on `getOwnedProjectIds` which must also be local.
    CRITICAL: The reactions handler (POST /api/messages/:messageId/reactions) uses `personalityEngine.seedProfileFromDB`, `personalityEngine.adaptPersonalityFromFeedback`, `personalityEngine.getPersonalityProfile`, `storage.getAgent`, `storage.updateAgent`, and `markSkillUsed`. All of these are imported at the module top level, not from routes.ts closures, so they work fine in the new module.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npm run typecheck && npm run test:integrity && npm run test:dto</automated>
  </verify>
  <acceptance_criteria>
    - `server/routes/messages.ts` exists and exports `registerMessageRoutes`
    - grep for `app.get("/api/conversations/:projectId"` in server/routes.ts returns ZERO matches
    - grep for `app.post("/api/messages"` in server/routes.ts returns ZERO matches (the REST endpoint; WS send_message is separate)
    - grep for `app.post('/api/messages/:messageId/reactions'` in server/routes.ts returns ZERO matches
    - grep for `app.post("/api/training/feedback"` in server/routes.ts returns ZERO matches
    - grep for `registerMessageRoutes(app)` in server/routes.ts returns exactly 1 match
    - `npm run typecheck` exits with code 0
    - `npm run test:integrity` exits with code 0
    - `npm run test:dto` exits with code 0
  </acceptance_criteria>
  <done>All conversation, message, reaction, and feedback REST routes live in messages.ts. routes.ts no longer contains any /api/conversations*, /api/messages*, or /api/training/feedback handlers. TypeScript compiles cleanly. test:integrity and test:dto pass.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npm run typecheck` passes
- `npm run test:integrity` passes
- `npm run test:dto` passes
- Three new files exist: `server/routes/teams.ts`, `server/routes/agents.ts`, `server/routes/messages.ts`
- routes.ts has shrunk by ~600 lines (teams 108 + agents 125 + messages 218 + reactions/feedback 123 = ~574 lines removed)
</verification>

<success_criteria>
- server/routes/teams.ts handles all /api/teams* routes
- server/routes/agents.ts handles all /api/agents* routes
- server/routes/messages.ts handles all /api/conversations*, /api/messages*, and /api/training/feedback routes
- `npm run typecheck && npm run test:integrity && npm run test:dto` exits cleanly
- routes.ts no longer contains any of these route handlers
</success_criteria>

<output>
After completion, create `.planning/phases/05-route-architecture-cleanup/05-01-SUMMARY.md`
</output>
