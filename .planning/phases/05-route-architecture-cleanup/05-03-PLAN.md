---
phase: 05-route-architecture-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - server/routes/chat.ts
  - server/routes.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02

must_haves:
  truths:
    - "POST /api/hatch/chat is handled by server/routes/chat.ts"
    - "WebSocket server setup and connection handling is in server/routes/chat.ts"
    - "handleStreamingColleagueResponse and handleMultiAgentResponse are in server/routes/chat.ts"
    - "server/routes.ts is reduced to imports, orchestrator registerRoutes(), auth routes, dev routes, and module registration calls"
    - "npm run typecheck passes with zero errors"
    - "npm run test:integrity and npm run test:dto pass"
    - "routes.ts is under 600 lines"
  artifacts:
    - path: "server/routes/chat.ts"
      provides: "WebSocket server, streaming AI handler, /api/hatch/chat endpoint"
      exports: ["registerChatRoutes"]
    - path: "server/routes.ts"
      provides: "Orchestrator that imports and registers all route modules"
      exports: ["registerRoutes", "getGlobalBroadcast"]
  key_links:
    - from: "server/routes.ts"
      to: "server/routes/chat.ts"
      via: "registerChatRoutes(app, httpServer, deps) call"
      pattern: "registerChatRoutes\\(app"
    - from: "server/routes/chat.ts"
      to: "server/routes.ts"
      via: "onBroadcastReady callback sets _globalBroadcast"
      pattern: "onBroadcastReady"
---

<objective>
Extract the WebSocket server, streaming AI handler, and /api/hatch/chat endpoint from `server/routes.ts` into `server/routes/chat.ts`. Then reduce routes.ts to a thin orchestrator that imports and registers all route modules. This is the largest and most complex extraction (~2,500 lines) due to the WS closures.

Purpose: Complete ARCH-01 (all 5 target modules extracted) and prove ARCH-02 (all tests pass after split). After this plan, routes.ts is a clean orchestrator under 600 lines.

Output: Fully populated `server/routes/chat.ts`, slim `routes.ts`, all tests green.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-route-architecture-cleanup/05-RESEARCH.md
@.planning/phases/05-route-architecture-cleanup/05-01-SUMMARY.md
@.planning/phases/05-route-architecture-cleanup/05-02-SUMMARY.md

<interfaces>
<!-- The chat.ts module is the most complex extraction because it contains: -->
<!-- 1. WS server creation (wss, activeConnections, streamingConversations, activeStreamingResponses) -->
<!-- 2. WS connection handler (join_conversation, send_message_streaming, cancel_streaming, typing) -->
<!-- 3. broadcastToConversation + broadcastToProject helper functions -->
<!-- 4. handleMultiAgentResponse (~270 lines) -->
<!-- 5. handleStreamingColleagueResponse (~1,630 lines) — the largest function in the codebase -->
<!-- 6. extractAndStoreMemory + extractUserName helper functions -->
<!-- 7. POST /api/hatch/chat endpoint -->

<!-- Pattern: chat.ts receives httpServer + sessionParser, creates WS server internally -->
<!-- It calls back to routes.ts via onBroadcastReady to set _globalBroadcast -->

From server/routes.ts (the broadcastToConversation closure at line 2060):
```typescript
function broadcastToConversation(conversationId: string, data: any, options?: { exclude?: WebSocket }) {
  const connections = activeConnections.get(conversationId);
  if (connections) {
    const message = JSON.stringify(data);
    connections.forEach(connection => {
      if (options?.exclude && connection === options.exclude) return;
      if (connection.readyState === WebSocket.OPEN) connection.send(message);
    });
  }
}
```

From server/routes.ts (the _globalBroadcast pattern at lines 89-92):
```typescript
let _globalBroadcast: ((conversationId: string, data: unknown) => void) | null = null;
export function getGlobalBroadcast(): ((conversationId: string, data: unknown) => void) | null {
  return _globalBroadcast;
}
```

Deps interface for chat.ts:
```typescript
export interface RegisterChatDeps {
  sessionParser?: (req: any, res: any, next: (err?: unknown) => void) => void;
  onBroadcastReady: (fns: {
    broadcastToConversation: (conversationId: string, data: unknown) => void;
    broadcastToProject: (projectId: string, data: unknown) => void;
  }) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract chat.ts — WS server, streaming handler, hatch/chat endpoint</name>
  <files>server/routes/chat.ts, server/routes.ts</files>
  <read_first>
    - server/routes.ts (full file — read in sections: lines 1-93 for imports/globals, lines 94-170 for sendWsError/getWsHealth/registerHealth, lines 1017-1044 for /api/hatch/chat, lines 1494-2079 for WS setup + broadcastToConversation, lines 2081-4045 for handleMultiAgentResponse + handleStreamingColleagueResponse + memory helpers)
    - server/routes/chat.ts (current stub — will be fully replaced)
    - server/index.ts (lines 1-20 for how registerRoutes and getGlobalBroadcast are imported)
  </read_first>
  <action>
    This is a large extraction. Follow this precise sequence.

    **Step 1: Design the chat.ts module signature**

    The module exports:
    ```typescript
    export interface RegisterChatDeps {
      sessionParser?: (req: any, res: any, next: (err?: unknown) => void) => void;
      onBroadcastReady: (fns: {
        broadcastToConversation: (conversationId: string, data: unknown) => void;
        broadcastToProject: (projectId: string, data: unknown) => void;
      }) => void;
    }

    export function registerChatRoutes(
      app: Express,
      httpServer: Server,
      deps: RegisterChatDeps
    ): { getWsHealth: () => { status: 'ok' | 'degraded' | 'down'; connections: number } }
    ```

    The return value provides `getWsHealth` so routes.ts can pass it to `registerHealthRoute`.

    **Step 2: Create server/routes/chat.ts**

    Move ALL of the following from routes.ts into this new file:

    A. **Imports** — Collect all imports used by the moved code. These include (at minimum):
       - `type { Express, Request } from 'express'`
       - `{ type Server } from 'http'` (receives httpServer, does NOT create it)
       - `{ WebSocketServer, WebSocket } from 'ws'`
       - `{ storage } from '../storage.js'`
       - ALL `@shared/*` imports used by WS/streaming code: `parseConversationId`, `buildConversationId`, `wsClientMessageSchema`
       - ALL `../ai/*` imports used by the streaming handler: `generateIntelligentResponse`, `generateStreamingResponse`, `OpenAIConfigurationError`, `getCharacterProfile`, `parseAction`, `stripActionBlocks`, `detectUserPermission`, `resolveMentionedAgent`, `applyTeammateToneGuard`, `personalityEngine`, `evaluateConductorDecision`, `buildRoleIdentity`, `evaluateSafetyScore`, `buildClarificationIntervention`, `buildDecisionForecast`, `isStrategicTurn`, `createActionProposal`, `recordLearningEvent`, `analyzeQuestion`, `findBestAgentMatch`, `coordinateMultiAgentResponse`, `calculateExpertiseConfidence`, `initiateHandoff`, `transferContext`, `processHandoffRequest`, `handoffTracker`, `resolveSpeakingAuthority`, `filterAvailableAgents`, `validateMessageIngress`
       - ALL `../autonomy/*` imports: `assertConversationOrdering`, `assertUniqueMessageId`, `checkIdempotencyKey`, `ensureMessageId`, `runPeerReview`, `resolveDecisionConflict`, `logAutonomyEvent`, `appendDeliberationRound`, `createDeliberationTrace`, `finalizeDeliberationTrace`, `BUDGETS`
       - `{ routeTools } from '../tools/toolRouter.js'`
       - `{ createTaskGraph } from '../autonomy/taskGraph/taskGraphEngine.js'`
       - `{ runAutonomousKnowledgeLoop } from '../knowledge/akl/runner.js'`
       - `{ assertPhase1Invariants } from '../invariants/assertPhase1.js'`
       - `{ getCurrentRuntimeConfig } from '../llm/providerResolver.js'`
       - `{ writeConfigSnapshot } from '../utils/configSnapshot.js'`
       - `{ markSkillUsed } from '../knowledge/skillUpdates/skillLearner.js'`
       - `{ z } from 'zod'`
       - `{ randomUUID } from 'crypto'`
       - `{ insertMessageSchema, type Task, type Project } from '@shared/schema'` (used in streaming handler for message creation)

    B. **Type definitions** — Move from routes.ts:
       - `type AuthedWebSocket = WebSocket & { __userId?: string };`

    C. **Helper re-declarations** — Inside `registerChatRoutes`, re-declare locally:
       - `devLog`
       - `getSessionUserId`
       - `getOwnedProjectIds`
       - `getOwnedProject`
       - `conversationOwnedByUser`
       - `isProd` (from `process.env.NODE_ENV === "production"`)
       - `sendWsError` function (lines 101-123 of routes.ts)

    D. **Move POST /api/hatch/chat** (routes.ts lines 1017-1044) — the non-streaming fallback endpoint.

    E. **Move WS server setup** (routes.ts lines 1497-2057):
       - `const wss = new WebSocketServer({ server: httpServer, path: '/ws' })`
       - `applySessionToWsRequest`
       - `activeConnections`, `streamingConversations`, `activeStreamingResponses`
       - `wait`, `waitForStreamingSlot`
       - The entire `wss.on('connection', ...)` handler
       - Build `getWsHealth` inside this function and return it

    F. **Move broadcastToConversation + broadcastToProject** (routes.ts lines 2060-2079 + the broadcastToProject added in plan 02):
       - After defining these, call `deps.onBroadcastReady({ broadcastToConversation, broadcastToProject })` so routes.ts can set `_globalBroadcast` and pass broadcast deps to projects.ts and tasks.ts.

    G. **Move handleMultiAgentResponse** (routes.ts lines 2081-2327).

    H. **Move handleStreamingColleagueResponse** (routes.ts lines 2329-3960) — the largest function (~1,630 lines).

    I. **Move extractAndStoreMemory + extractUserName helpers** (routes.ts lines 3962-4045).

    **Step 3: Update server/routes.ts to be the orchestrator**

    After extraction, routes.ts should contain ONLY:
    1. Imports for route module registrations and remaining code
    2. `_globalBroadcast` + `getGlobalBroadcast()` export (unchanged — set via callback)
    3. `export async function registerRoutes(app, sessionParser)`:
       a. `initializePreTrainedColleagues()` call
       b. `const httpServer = createServer(app)` — httpServer creation stays in routes.ts
       c. `requireAuth` middleware + `app.use('/api', requireAuth)` registration
       d. `getSessionUserId` (local, for auth routes)
       e. `app.get("/api/system/storage-status", ...)` dev-only route
       f. Auth routes (Google OAuth — lines 1171-1353: `/api/auth/google/start`, `/api/auth/google/callback`, `/api/auth/me`, `/api/auth/logout`)
       g. Dev/personality/handoff routes (lines 1354-1492)
       h. `registerAutonomyRoutes(app)` call
       i. Registration of chat module — BEFORE projects/tasks since broadcast must be ready:
          ```typescript
          let broadcastToConversation: (cid: string, data: unknown) => void = () => {};
          let broadcastToProject: (pid: string, data: unknown) => void = () => {};

          const { getWsHealth } = registerChatRoutes(app, httpServer, {
            sessionParser,
            onBroadcastReady: (fns) => {
              broadcastToConversation = fns.broadcastToConversation;
              broadcastToProject = fns.broadcastToProject;
              _globalBroadcast = (conversationId, data) => fns.broadcastToConversation(conversationId, data);
            },
          });
          ```
       j. Update `registerHealthRoute(app, { getWsHealth: () => getWsHealth() })` — can stay as-is since getWsHealth is available after chat registration.
       k. `registerTeamRoutes(app)` call
       l. `registerAgentRoutes(app)` call
       m. `registerMessageRoutes(app)` call
       n. `registerProjectRoutes(app, { broadcastToConversation })` call
       o. `registerTaskRoutes(app, { broadcastToConversation, broadcastToProject })` call
       p. `return httpServer`

    **Critical ordering:** The `registerHealthRoute(app, ...)` call currently exists near the top of registerRoutes. After refactoring, it needs to be called AFTER `registerChatRoutes` provides `getWsHealth`. Move the health route registration to after the chat module registration.

    **What stays in routes.ts (leave untouched):**
    - Auth routes (lines 1171-1353): `/api/auth/google/start`, `/api/auth/google/callback`, `/api/auth/me`, `/api/auth/logout`
    - Dev/personality/handoff routes (lines 1354-1492): `/api/dev/training/*`, personality, handoff stats
    - `app.get("/api/system/storage-status", ...)` dev-only route
    - `requireAuth` middleware and its `app.use('/api', ...)` registration
    - The `sanitizeReturnTo` and `regenerateSession` helpers used by auth routes

    CRITICAL: Do NOT change any function signatures or logic in the moved code. This is pure extraction.
    CRITICAL: `httpServer` is created in routes.ts and passed to `registerChatRoutes`. Do NOT create httpServer inside chat.ts.
    CRITICAL: The `SessionParser` type alias is used by routes.ts (for the function signature) — keep it in routes.ts. Pass `sessionParser` as part of deps.
    CRITICAL: Remove the old `registerChatRoutes` stub import if it exists and replace with the real import.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `server/routes/chat.ts` exports `registerChatRoutes` function (not the empty stub)
    - `server/routes/chat.ts` exports `RegisterChatDeps` interface
    - wc -l of server/routes/chat.ts is approximately 2500-3000 lines
    - wc -l of server/routes.ts is under 600 lines
    - grep for `handleStreamingColleagueResponse` in server/routes.ts returns ZERO matches
    - grep for `handleMultiAgentResponse` in server/routes.ts returns ZERO matches
    - grep for `new WebSocketServer` in server/routes.ts returns ZERO matches
    - grep for `activeConnections` in server/routes.ts returns ZERO matches
    - grep for `registerChatRoutes(app` in server/routes.ts returns exactly 1 match
    - grep for `onBroadcastReady` in server/routes/chat.ts returns at least 1 match
    - grep for `getGlobalBroadcast` in server/routes.ts returns exactly 1 match (the export)
    - `npm run typecheck` exits with code 0
  </acceptance_criteria>
  <done>chat.ts contains all WS setup, streaming handler, and /api/hatch/chat. routes.ts is a thin orchestrator under 600 lines with only auth, dev, and module registration. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Final validation — full test suite + line count verification</name>
  <files>server/routes.ts</files>
  <read_first>
    - server/routes.ts (full file — verify it's a clean orchestrator)
    - server/routes/chat.ts (first 30 lines — verify exports)
    - server/routes/projects.ts (first 10 lines — verify exports)
    - server/routes/tasks.ts (first 10 lines — verify exports)
    - server/routes/teams.ts (first 10 lines — verify exports)
    - server/routes/agents.ts (first 10 lines — verify exports)
    - server/routes/messages.ts (first 10 lines — verify exports)
  </read_first>
  <action>
    **Run the full validation suite:**

    1. `npm run typecheck` — TypeScript compilation must pass with zero errors.
    2. `npm run test:integrity` — Message ordering integrity tests must pass unchanged.
    3. `npm run test:dto` — DTO contract validation must pass unchanged.
    4. `wc -l server/routes.ts` — Must be under 600 lines.
    5. Verify all 7 route module files exist in `server/routes/`:
       - `health.ts` (pre-existing)
       - `autonomy.ts` (pre-existing)
       - `chat.ts` (extracted in Task 1)
       - `projects.ts` (extracted in Plan 02)
       - `teams.ts` (extracted in Plan 01)
       - `agents.ts` (extracted in Plan 01)
       - `messages.ts` (extracted in Plan 01)
       - `tasks.ts` (extracted in Plan 02)

    **If any test fails:** The failure is a regression from the extraction. Read the error output, identify which import or function reference is missing, and fix it. Do NOT change any business logic — only fix import paths, missing type references, or missing helper re-declarations.

    **If typecheck fails:** The most common causes will be:
    - Missing import in a new module (add the import)
    - Unused import in routes.ts after deletion (remove the import)
    - Type mismatch from `any` cast needed for session (add `as any` cast matching original)

    **Verify routes.ts is a clean orchestrator by confirming it contains:**
    - Module imports (registerXxxRoutes from ./routes/xxx.js)
    - `_globalBroadcast` / `getGlobalBroadcast` export
    - `registerRoutes` export function
    - `requireAuth` middleware
    - Auth routes (google OAuth)
    - Dev/personality routes
    - Module registration calls
    - `return httpServer`
    - And NOTHING ELSE (no route handlers for projects/teams/agents/messages/tasks/chat)
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npm run typecheck && npm run test:integrity && npm run test:dto</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits with code 0
    - `npm run test:integrity` exits with code 0
    - `npm run test:dto` exits with code 0
    - wc -l of server/routes.ts is under 600
    - ls server/routes/ shows at least 8 .ts files (health, autonomy, chat, projects, teams, agents, messages, tasks)
    - grep for `app.get("/api/projects"` in server/routes.ts returns ZERO matches
    - grep for `app.get("/api/teams"` in server/routes.ts returns ZERO matches
    - grep for `app.get("/api/agents"` in server/routes.ts returns ZERO matches
    - grep for `app.get("/api/conversations` in server/routes.ts returns ZERO matches
    - grep for `app.get("/api/tasks"` in server/routes.ts returns ZERO matches
    - grep for `handleStreamingColleagueResponse` in server/routes.ts returns ZERO matches
  </acceptance_criteria>
  <done>Full test suite green. routes.ts is a clean orchestrator under 600 lines. All 5 required modules (projects, teams, agents, messages, chat) exist and handle their respective routes. ARCH-01 and ARCH-02 are fully satisfied.</done>
</task>

</tasks>

<verification>
Phase 5 success criteria verification:
1. `server/routes/projects.ts` exists and handles all `/api/projects*` routes — YES (Plan 02)
2. `server/routes/teams.ts` exists and handles all `/api/teams*` routes — YES (Plan 01)
3. `server/routes/agents.ts` exists and handles all `/api/agents*` routes — YES (Plan 01)
4. `server/routes/messages.ts` exists and handles all `/api/conversations*` and `/api/messages*` routes — YES (Plan 01)
5. `server/routes/chat.ts` exists and handles `/api/hatch/chat` and WebSocket message handlers — YES (Plan 03)
6. `npm run typecheck` passes with zero errors — YES
7. `npm run test:integrity` passes — YES
8. `npm run test:dto` passes — YES
9. Original `routes.ts` reduced to only imports/registration — YES (under 600 lines)
</verification>

<success_criteria>
- chat.ts contains WS server, streaming handler, /api/hatch/chat endpoint
- routes.ts is reduced to orchestrator: imports, auth, dev, module registrations (~500-600 lines)
- All 3 test commands pass: typecheck, test:integrity, test:dto
- Zero behavior changes from pre-extraction state
- ARCH-01 fully satisfied (5 modules extracted)
- ARCH-02 fully satisfied (all tests pass)
</success_criteria>

<output>
After completion, create `.planning/phases/05-route-architecture-cleanup/05-03-SUMMARY.md`
</output>
