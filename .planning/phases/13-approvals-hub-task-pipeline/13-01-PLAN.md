---
phase: 13-approvals-hub-task-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/sidebar/approvalUtils.ts
  - client/src/components/sidebar/ApprovalItem.tsx
  - client/src/components/sidebar/ApprovalsEmptyState.tsx
  - scripts/test-approval-expiry.test.ts
  - scripts/test-task-pipeline-view.test.ts
autonomous: true
requirements: [APPR-01, APPR-03, APPR-04]

must_haves:
  truths:
    - "Pending approval items render with agent name, risk reasons, and Approve Task / Reject Task buttons"
    - "Expired approvals show an 'Expired' badge instead of action buttons"
    - "Empty state shows 'Nothing needs your approval' with ShieldCheck icon when no approvals exist"
  artifacts:
    - path: "client/src/components/sidebar/approvalUtils.ts"
      provides: "isApprovalExpired pure function + APPROVAL_EXPIRY_MS constant + PIPELINE_STAGES constant"
      exports: ["isApprovalExpired", "APPROVAL_EXPIRY_MS", "PIPELINE_STAGES"]
    - path: "client/src/components/sidebar/ApprovalItem.tsx"
      provides: "Single approval row with approve/reject mutations and expiry display"
      exports: ["ApprovalItem"]
    - path: "client/src/components/sidebar/ApprovalsEmptyState.tsx"
      provides: "Empty state wrapper using EmptyState component"
      exports: ["ApprovalsEmptyState"]
    - path: "scripts/test-approval-expiry.test.ts"
      provides: "Unit tests for isApprovalExpired"
    - path: "scripts/test-task-pipeline-view.test.ts"
      provides: "Unit tests for PIPELINE_STAGES stage mapping (APPR-02 Wave 0 file)"
  key_links:
    - from: "client/src/components/sidebar/ApprovalItem.tsx"
      to: "/api/tasks/:id/approve and /api/tasks/:id/reject"
      via: "TanStack useMutation fetch calls"
      pattern: "fetch.*api/tasks.*approve|reject"
    - from: "client/src/components/sidebar/ApprovalItem.tsx"
      to: "client/src/components/sidebar/approvalUtils.ts"
      via: "import isApprovalExpired"
      pattern: "isApprovalExpired"
---

<objective>
Create the leaf components for the Approvals tab: the approval expiry utility, the ApprovalItem card with approve/reject mutations, and the empty state. These are the foundational pieces that Plan 02 will compose into the full ApprovalsTab container.

Purpose: Establish the visual and interactive building blocks for the approvals surface — each piece is independently testable and follows established sidebar patterns.
Output: 3 source files + 2 test files in sidebar/ and scripts/
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-approvals-hub-task-pipeline/13-RESEARCH.md
@.planning/phases/13-approvals-hub-task-pipeline/13-UI-SPEC.md
@.planning/phases/13-approvals-hub-task-pipeline/13-VALIDATION.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From client/src/components/ui/EmptyState.tsx:
```typescript
interface EmptyStateProps {
  icon: LucideIcon;
  title: string;
  description: string;
  action?: { label: string; onClick: () => void };
}
export function EmptyState({ icon: Icon, title, description, action }: EmptyStateProps) { ... }
```

From shared/schema.ts (tasks table — lines 188-215):
```typescript
export const tasks = pgTable("tasks", {
  id: varchar("id").primaryKey(),
  title: text("title").notNull(),
  status: text("status").notNull().$type<"todo" | "in_progress" | "completed" | "blocked">().default("todo"),
  assignee: text("assignee"),
  metadata: jsonb("metadata").$type<{
    createdFromChat?: boolean; messageId?: string;
    estimatedHours?: number; actualHours?: number;
  }>().default({}),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
  // ... other fields
});
// NOTE: metadata also contains runtime fields NOT in schema type:
// awaitingApproval?: boolean, approvedAt?: string, rejectedAt?: string,
// riskScore?: number, riskReasons?: string[], draftOutput?: string
```

From client/src/lib/autonomyEvents.ts:
```typescript
export const AUTONOMY_EVENTS = {
  APPROVAL_REQUIRED: 'autonomy:approval_required',
  // ...
} as const;
```

From client/src/components/sidebar/ActivityFeedItem.tsx (visual pattern):
- 3px color-coded left border: `border-l-[3px]`
- 32px agent avatar circle with initial letter
- text-xs for labels, text-[10px] for timestamps
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create approval expiry utility + unit tests</name>
  <files>client/src/components/sidebar/approvalUtils.ts, scripts/test-approval-expiry.test.ts, scripts/test-task-pipeline-view.test.ts</files>
  <read_first>
    - shared/schema.ts (lines 188-215 for tasks table shape)
    - .planning/phases/13-approvals-hub-task-pipeline/13-RESEARCH.md (Pattern 3: Expiry Derivation, Pattern 4: Pipeline Stage Mapping)
  </read_first>
  <behavior>
    - isApprovalExpired returns false for a task with awaitingApproval=true and updatedAt within 30 minutes
    - isApprovalExpired returns true for a task with awaitingApproval=true and updatedAt older than 30 minutes
    - isApprovalExpired returns false for a task with awaitingApproval=false (not an approval task)
    - isApprovalExpired returns false for a task with approvedAt set (already actioned)
    - isApprovalExpired returns false for a task with rejectedAt set (already actioned)
    - PIPELINE_STAGES maps status "todo" without assignee to "Queued"
    - PIPELINE_STAGES maps status "todo" with assignee to "Assigned"
    - PIPELINE_STAGES maps status "in_progress" to "In Progress"
    - PIPELINE_STAGES maps status "blocked" to "Review" (NOT raw "blocked") — this is the APPR-02 coverage case
    - PIPELINE_STAGES maps status "completed" to "Done"
  </behavior>
  <action>
    Create `client/src/components/sidebar/approvalUtils.ts` with:

    1. `APPROVAL_EXPIRY_MS` constant set to `30 * 60 * 1000` (30 minutes)

    2. `isApprovalExpired(task: { metadata: unknown; updatedAt: string | Date }): boolean` function:
       - Cast metadata to `Record<string, unknown>`
       - Return false if `metadata?.awaitingApproval` is not `true`
       - Return false if `metadata?.approvedAt` or `metadata?.rejectedAt` is truthy
       - Return `Date.now() - new Date(task.updatedAt).getTime() > APPROVAL_EXPIRY_MS`

    3. `PIPELINE_STAGES` constant array with 5 stages:
       - `{ id: 'queued', label: 'Queued', dot: 'bg-gray-400', filter: (t) => t.status === 'todo' && !t.assignee }`
       - `{ id: 'assigned', label: 'Assigned', dot: 'bg-[var(--hatchin-blue)]', filter: (t) => t.status === 'todo' && !!t.assignee }`
       - `{ id: 'inprogress', label: 'In Progress', dot: 'bg-[var(--hatchin-orange)] animate-pulse', filter: (t) => t.status === 'in_progress' }`
       - `{ id: 'review', label: 'Review', dot: 'bg-amber-400', filter: (t) => t.status === 'blocked' }` — comment: "blocked" maps to "Review" display label
       - `{ id: 'done', label: 'Done', dot: 'bg-[var(--hatchin-green)]', filter: (t) => t.status === 'completed' }`

    Then create `scripts/test-approval-expiry.test.ts` with vitest tests covering the isApprovalExpired behaviors listed above. Use `vi.useFakeTimers()` to control Date.now() for expiry tests.

    Then create `scripts/test-task-pipeline-view.test.ts` with vitest tests covering the PIPELINE_STAGES behaviors listed above. This is the Wave 0 test file declared in VALIDATION.md for APPR-02 coverage. For each stage, create a mock task object with the matching status/assignee combination and assert that the stage's filter function returns true. Also assert that the "blocked" → "Review" mapping exists by finding the stage with id "review" and confirming its label is "Review" (not "blocked") — this is the key APPR-02 assertion. Import PIPELINE_STAGES directly from `../client/src/components/sidebar/approvalUtils`.
  </action>
  <verify>
    <automated>npx vitest run scripts/test-approval-expiry.test.ts scripts/test-task-pipeline-view.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isApprovalExpired" client/src/components/sidebar/approvalUtils.ts` returns at least 1
    - `grep -c "APPROVAL_EXPIRY_MS" client/src/components/sidebar/approvalUtils.ts` returns at least 1
    - `grep -c "PIPELINE_STAGES" client/src/components/sidebar/approvalUtils.ts` returns at least 1
    - `grep "Review" client/src/components/sidebar/approvalUtils.ts` confirms "Review" label exists (not "blocked")
    - `npx vitest run scripts/test-approval-expiry.test.ts scripts/test-task-pipeline-view.test.ts` passes all tests
  </acceptance_criteria>
  <done>isApprovalExpired correctly identifies expired/active/actioned approvals. PIPELINE_STAGES maps all 5 DB status values to UI labels including blocked → "Review". Both test files pass (isApprovalExpired tests + PIPELINE_STAGES stage mapping tests).</done>
</task>

<task type="auto">
  <name>Task 2: Create ApprovalItem and ApprovalsEmptyState components</name>
  <files>client/src/components/sidebar/ApprovalItem.tsx, client/src/components/sidebar/ApprovalsEmptyState.tsx</files>
  <read_first>
    - client/src/components/AutonomousApprovalCard.tsx (visual sibling — match button styles, animation values)
    - client/src/components/sidebar/ActivityFeedItem.tsx (sidebar card pattern — 3px border, avatar, text hierarchy)
    - client/src/components/ui/EmptyState.tsx (EmptyState interface)
    - client/src/components/sidebar/approvalUtils.ts (just created in Task 1)
    - .planning/phases/13-approvals-hub-task-pipeline/13-UI-SPEC.md (exact copy, colors, animations)
  </read_first>
  <action>
    Create `client/src/components/sidebar/ApprovalItem.tsx`:

    Props: `{ task: Task }` where Task is from `@shared/schema`.

    Implementation:
    - Import `isApprovalExpired` from `./approvalUtils`
    - Import `useMutation`, `useQueryClient` from `@tanstack/react-query`
    - Import `useToast` from `@/hooks/use-toast`
    - Import `motion` from `framer-motion`
    - Import `CheckCircle`, `XCircle` from `lucide-react`

    - Cast `task.metadata as Record<string, unknown>` (NOT `as any` — per research pitfall 2)
    - Derive `isExpired` from `isApprovalExpired(task)`
    - Create `approveMutation` with `useMutation`:
      - `mutationFn`: `fetch(\`/api/tasks/${task.id}/approve\`, { method: 'POST' })` then check `r.ok`
      - `onSuccess`: `queryClient.invalidateQueries({ queryKey: ['/api/tasks'] })`
      - `onError`: `toast({ description: "Couldn't process your decision. Try again.", variant: "destructive" })`
    - Create `rejectMutation` with same pattern but `/reject` endpoint
    - `isLoading = approveMutation.isPending || rejectMutation.isPending`
    - `agentName = task.assignee ?? 'Hatch'`
    - `riskScore = meta?.riskScore as number | undefined`
    - Border color: `riskScore && riskScore >= 0.6 ? 'border-l-[var(--hatchin-orange)]' : 'border-l-amber-400'`

    Render as `motion.div` with:
    - `initial={{ opacity: 0, y: 8 }}`
    - `animate={{ opacity: 1, y: 0 }}`
    - `exit={{ opacity: 0, y: -4 }}`
    - `transition={{ duration: isExpired ? 0 : 0.18, ease: 'easeOut' }}`
    - className: `flex items-start gap-2.5 px-3 py-3 rounded-lg bg-[var(--hatchin-surface)] border-l-[3px] ${borderColor}`

    Inside:
    - 32px avatar circle with initial letter (w-8 h-8 rounded-full)
    - Agent name: `text-sm font-semibold text-[var(--hatchin-text-bright)]`
    - Risk reasons (if `meta.riskReasons` is an array with length > 0): joined with " · ", text-xs text-muted-foreground
    - If expired: render `<span>` with "Expired" text, `aria-label="Approval expired"`, classes `inline-flex px-2 py-0.5 text-xs rounded-md bg-red-500/10 text-red-400`
    - If not expired: render two buttons:
      - Approve: `bg-[var(--hatchin-blue)] text-white`, CheckCircle icon 3.5w, text "Approve Task", `aria-label={\`Approve task: ${task.title}\`}`
      - Reject: `border border-red-500/30 text-red-400`, XCircle icon 3.5w, text "Reject Task", `aria-label={\`Reject task: ${task.title}\`}`
      - Both: `disabled={isLoading}`, `disabled:opacity-50 disabled:cursor-not-allowed`, `type="button"`, `px-3 py-1.5 text-xs font-medium rounded-lg`

    Create `client/src/components/sidebar/ApprovalsEmptyState.tsx`:

    - Import `EmptyState` from `@/components/ui/EmptyState`
    - Import `ShieldCheck` from `lucide-react`
    - Export `ApprovalsEmptyState` that renders:
      ```tsx
      <div aria-live="polite">
        <EmptyState
          icon={ShieldCheck}
          title="Nothing needs your approval"
          description="When a Hatch starts something that needs a sign-off, it'll show up here. You're in control of what gets acted on."
        />
      </div>
      ```
    - No action/CTA button — approvals are system-generated.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Approve Task" client/src/components/sidebar/ApprovalItem.tsx` finds the button text
    - `grep "Reject Task" client/src/components/sidebar/ApprovalItem.tsx` finds the button text
    - `grep "Expired" client/src/components/sidebar/ApprovalItem.tsx` finds the expired badge text
    - `grep "isApprovalExpired" client/src/components/sidebar/ApprovalItem.tsx` confirms expiry util is imported
    - `grep "Nothing needs your approval" client/src/components/sidebar/ApprovalsEmptyState.tsx` finds the exact empty state heading
    - `grep "ShieldCheck" client/src/components/sidebar/ApprovalsEmptyState.tsx` confirms correct icon
    - `grep "aria-live" client/src/components/sidebar/ApprovalsEmptyState.tsx` confirms accessibility
    - `npm run typecheck` passes
  </acceptance_criteria>
  <done>ApprovalItem renders pending approvals with one-click approve/reject, shows expired badge for stale items. ApprovalsEmptyState shows branded empty state with ShieldCheck icon. Both pass typecheck.</done>
</task>

</tasks>

<verification>
- `npx vitest run scripts/test-approval-expiry.test.ts scripts/test-task-pipeline-view.test.ts` — all expiry and pipeline stage tests pass
- `npm run typecheck` — no type errors
- All 5 files exist: approvalUtils.ts, ApprovalItem.tsx, ApprovalsEmptyState.tsx, test-approval-expiry.test.ts, test-task-pipeline-view.test.ts
</verification>

<success_criteria>
- Pure function `isApprovalExpired` correctly classifies task approval states with unit test coverage
- `PIPELINE_STAGES` maps all 5 DB status values to human-readable labels (including `blocked` -> "Review")
- `scripts/test-task-pipeline-view.test.ts` covers the blocked → "Review" mapping for APPR-02 (Wave 0 requirement)
- `ApprovalItem` renders with approve/reject buttons that call the existing backend endpoints via TanStack mutations
- `ApprovalsEmptyState` uses the shared `EmptyState` component with exact copy from UI-SPEC
- All files pass typecheck
</success_criteria>

<output>
After completion, create `.planning/phases/13-approvals-hub-task-pipeline/13-01-SUMMARY.md`
</output>
