---
phase: 11-sidebar-shell-activity-feed
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - client/src/components/sidebar/ActivityTab.tsx
  - client/src/components/sidebar/ActivityFeedItem.tsx
  - client/src/components/sidebar/AutonomyStatsCard.tsx
  - client/src/components/sidebar/FeedFilters.tsx
  - client/src/components/avatars/BaseAvatar.tsx
  - client/src/index.css
  - client/src/components/LeftSidebar.tsx
  - client/src/components/ProjectTree.tsx
autonomous: true
requirements: [FEED-01, FEED-02, FEED-03, FEED-04, FEED-05, AGNT-01, SIDE-03]

must_haves:
  truths:
    - "User sees a live activity feed with agent avatar, event description, relative timestamp, and color-coded left border per category"
    - "User sees a stats card showing tasks completed, handoffs, and cost spent today"
    - "User can filter the feed by event type, agent, or time range using filter chips"
    - "Rapid events sharing a traceId are grouped into single feed items"
    - "Agent avatars show a rotating dashed ring animation when that agent is executing background work"
    - "Empty state displays when no autonomy events exist"
  artifacts:
    - path: "client/src/components/sidebar/ActivityTab.tsx"
      provides: "Complete Activity tab container with stats card, filters, feed list, and empty state"
      exports: ["ActivityTab"]
    - path: "client/src/components/sidebar/ActivityFeedItem.tsx"
      provides: "Single feed event row with avatar, description, timestamp, expandable detail, color border"
      exports: ["ActivityFeedItem"]
    - path: "client/src/components/sidebar/AutonomyStatsCard.tsx"
      provides: "Stats summary card with 3 stat pills"
      exports: ["AutonomyStatsCard"]
    - path: "client/src/components/sidebar/FeedFilters.tsx"
      provides: "Filter chip bar with event type, agent, and time filters"
      exports: ["FeedFilters"]
    - path: "client/src/components/avatars/BaseAvatar.tsx"
      provides: "Extended AvatarState with 'working' state and rotating ring overlay"
  key_links:
    - from: "client/src/components/sidebar/ActivityTab.tsx"
      to: "client/src/hooks/useAutonomyFeed.ts"
      via: "useAutonomyFeed hook"
      pattern: "import.*useAutonomyFeed"
    - from: "client/src/components/LeftSidebar.tsx"
      to: "client/src/hooks/useAgentWorkingState.ts"
      via: "useAgentWorkingState hook for avatar state"
      pattern: "import.*useAgentWorkingState"
    - from: "client/src/components/avatars/BaseAvatar.tsx"
      to: "client/src/index.css"
      via: "agent-working-ring keyframe animation"
      pattern: "agent-working-ring"
---

<objective>
Build the Activity tab content (stats card, filter chips, feed item list, empty state) and add the "working" avatar animation state. These are the visible UI components that users interact with.

Purpose: The Activity tab is the primary new surface in Phase 11. The feed shows real-time autonomy events, the stats card gives a dashboard summary, filters let users focus, and the working avatar provides ambient awareness of background execution.
Output: 4 new sidebar components, modified BaseAvatar with "working" state, CSS keyframes, LeftSidebar + ProjectTree working state integration
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sidebar-shell-activity-feed/11-CONTEXT.md
@.planning/phases/11-sidebar-shell-activity-feed/11-RESEARCH.md
@.planning/phases/11-sidebar-shell-activity-feed/11-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From client/src/hooks/useAutonomyFeed.ts:
```typescript
export interface FeedEvent {
  id: string;
  traceId: string;
  eventType: string;
  agentId: string | null;
  agentName: string | null;
  label: string;
  category: 'task' | 'handoff' | 'review' | 'approval' | 'system';
  timestamp: string;
  expandableData?: Record<string, unknown>;
}

export interface FeedStats {
  tasksCompleted: number;
  handoffs: number;
  costToday: string;
}

export function useAutonomyFeed(projectId: string | undefined): {
  events: FeedEvent[];
  stats: FeedStats | undefined;
  isLoading: boolean;
  unreadCount: number;
  clearUnread: () => void;
  activeFilter: string;
  setActiveFilter: (f: string) => void;
  agentFilter: string | null;
  setAgentFilter: (a: string | null) => void;
  timeFilter: string;
  setTimeFilter: (t: string) => void;
}
```

From client/src/hooks/useAgentWorkingState.ts:
```typescript
export function useAgentWorkingState(): Set<string>
```

From client/src/lib/autonomyEvents.ts:
```typescript
export const AUTONOMY_EVENTS = { ... } as const;
export type AutonomyEventName = ...;
```

<!-- Existing code patterns -->
From client/src/components/avatars/BaseAvatar.tsx:
```typescript
export type AvatarState = "idle" | "thinking" | "speaking" | "celebrating";
export interface AvatarProps { state: AvatarState; size?: number; className?: string; }
export function AvatarWrapper({ state, size, className, children }: AvatarWrapperProps) { ... }
// AvatarWrapper adds outer div with Framer Motion variants
```

From client/src/index.css (existing keyframes):
```css
@keyframes ai-thinking-ring { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
@keyframes brain-glow-pulse { ... }
@keyframes shimmer { ... }
@keyframes online-pulse { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ActivityTab, AutonomyStatsCard, FeedFilters, and ActivityFeedItem components</name>
  <files>client/src/components/sidebar/ActivityTab.tsx, client/src/components/sidebar/ActivityFeedItem.tsx, client/src/components/sidebar/AutonomyStatsCard.tsx, client/src/components/sidebar/FeedFilters.tsx</files>
  <read_first>
    - client/src/hooks/useAutonomyFeed.ts (from Plan 01 — full return type, FeedEvent and FeedStats interfaces)
    - client/src/components/sidebar/SidebarTabBar.tsx (from Plan 02 — established styling patterns in sidebar directory)
    - client/src/components/ui/EmptyState.tsx (from Plan 02 — EmptyState component props)
    - client/src/components/MessageBubble.tsx (lines 1-50 — avatar rendering pattern for agent avatars in messages)
    - client/src/components/avatars/AgentAvatar.tsx (how agent avatars are rendered with role prop)
    - client/src/index.css (existing shimmer keyframe for loading skeletons)
    - tailwind.config.ts (custom color tokens: hatchin-green, hatchin-blue, hatchin-orange, hatchin-surface)
    - client/src/lib/autonomyEvents.ts (from Plan 01 — event constants)
  </read_first>
  <action>
**File 1: `client/src/components/sidebar/AutonomyStatsCard.tsx`** (NEW)

Stats summary card with 3 stat pills:

```typescript
import { CheckCircle, ArrowRightLeft, DollarSign } from 'lucide-react';
import type { FeedStats } from '@/hooks/useAutonomyFeed';

interface AutonomyStatsCardProps {
  stats: FeedStats | undefined;
  isLoading: boolean;
}
```

- Outer container: `rounded-xl border border-[var(--hatchin-border-subtle)] bg-[var(--hatchin-surface)] p-3 mb-3`
- 3 stat pills in a row: `flex items-center gap-3` (flex, not grid — allows natural spacing)
- Each pill: `flex items-center gap-1.5 text-xs`
  - Icon: 14px size, color matching category
  - Number: `font-semibold hatchin-text` (bold number)
  - Label: `hatchin-text-muted` (muted label text)
- Pill 1: CheckCircle icon (hatchin-green) + `stats.tasksCompleted` + "tasks"
- Pill 2: ArrowRightLeft icon (hatchin-blue) + `stats.handoffs` + "handoffs"
- Pill 3: DollarSign icon (hatchin-text-muted) + `stats.costToday` + "spent"
- When `isLoading`: render 3 shimmer placeholder divs: `h-4 w-16 rounded bg-[var(--hatchin-surface-hover)] animate-shimmer`
- When `stats` undefined and not loading: render all zeros ("0 tasks", "0 handoffs", "$0.00 spent")

**File 2: `client/src/components/sidebar/FeedFilters.tsx`** (NEW)

Horizontal scrollable filter chip bar:

```typescript
interface FeedFiltersProps {
  activeFilter: string;
  onFilterChange: (filter: string) => void;
  agentFilter: string | null;
  onAgentFilterChange: (agentId: string | null) => void;
  timeFilter: string;
  onTimeFilterChange: (time: string) => void;
  agents: Array<{ id: string; name: string; role: string }>;
}
```

- Outer: `flex flex-wrap gap-1.5 mb-3 px-0.5`
- Event type chips array: `[{ id: 'all', label: 'All' }, { id: 'task', label: 'Tasks' }, { id: 'handoff', label: 'Handoffs' }, { id: 'review', label: 'Reviews' }, { id: 'approval', label: 'Approvals' }]`
- Each chip button:
  - Active: `bg-[var(--hatchin-blue)] text-white text-[11px] px-2.5 py-1 rounded-full font-medium`
  - Inactive: `border border-[var(--hatchin-border-subtle)] hatchin-text-muted text-[11px] px-2.5 py-1 rounded-full hover:border-[var(--hatchin-blue)] hover:text-[var(--hatchin-blue)] transition-colors`
- Time filter: 3 chips below event chips — "Today", "7 days", "All time". Same styling pattern. Active chip filled.
  - Chip values: `'today'`, `'7days'`, `'all'`
- Agent filter: If `agents.length > 0`, render a small `<select>` dropdown styled with Tailwind: `text-[11px] px-2 py-1 rounded-full border border-[var(--hatchin-border-subtle)] bg-transparent hatchin-text`
  - First option: "All agents" (value "")
  - Each agent: `<option value={agent.id}>{agent.name}</option>`
  - On change: call `onAgentFilterChange(value || null)`

**File 3: `client/src/components/sidebar/ActivityFeedItem.tsx`** (NEW)

Single event row with GitHub-notification-center styling:

```typescript
import { useState } from 'react';
import { motion } from 'framer-motion';
import { ChevronDown, ChevronRight } from 'lucide-react';
import type { FeedEvent } from '@/hooks/useAutonomyFeed';

interface ActivityFeedItemProps {
  event: FeedEvent;
}
```

Layout:
- Outer `<motion.div>` with entrance animation: `initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.18, ease: 'easeOut' }}`
- Container: `flex items-start gap-2.5 py-2.5 px-3 rounded-lg hover:bg-[var(--hatchin-surface-hover)] transition-colors cursor-pointer group`
- Color-coded left border (3px, rounded): use a `<div className="w-[3px] self-stretch rounded-full shrink-0" />` with background color:
  - `category === 'task'`: `bg-[var(--hatchin-green)]`
  - `category === 'handoff'`: `bg-[var(--hatchin-blue)]`
  - `category === 'review'`: `bg-[var(--hatchin-orange)]`
  - `category === 'approval'`: `bg-amber-400`
  - `category === 'system'`: `bg-gray-400`
- Agent avatar area: 32px circle. If `event.agentName` exists, show first letter of name in a colored circle. If not, show a gray system icon.
  - Circle: `w-8 h-8 rounded-full bg-[var(--hatchin-surface)] flex items-center justify-center text-xs font-semibold hatchin-text shrink-0`
- Content area: `flex-1 min-w-0`
  - Label: `<p className="text-xs hatchin-text leading-snug truncate">{event.label}</p>`
  - Timestamp: `<p className="text-[10px] hatchin-text-muted mt-0.5">{formatRelativeTime(event.timestamp)}</p>`
- Expandable detail: `useState(false)` for expanded state. If `event.expandableData` exists:
  - Show chevron icon (right when collapsed, down when expanded): `<ChevronRight className="w-3.5 h-3.5 hatchin-text-muted shrink-0 group-hover:opacity-100 opacity-0 transition-opacity" />`
  - On click toggle expanded
  - When expanded: render `<div className="mt-2 text-[11px] hatchin-text-muted bg-[var(--hatchin-surface)] rounded-lg p-2.5 space-y-1">` with key-value pairs from expandableData (risk score, peer review outcome, handoff context)

Add a `formatRelativeTime` helper inside the file:
```typescript
function formatRelativeTime(isoString: string): string {
  const now = Date.now();
  const then = new Date(isoString).getTime();
  const diffMs = now - then;
  const diffMin = Math.floor(diffMs / 60000);
  if (diffMin < 1) return 'just now';
  if (diffMin < 60) return `${diffMin}m ago`;
  const diffHr = Math.floor(diffMin / 60);
  if (diffHr < 24) return `${diffHr}h ago`;
  const diffDay = Math.floor(diffHr / 24);
  return `${diffDay}d ago`;
}
```

**File 4: `client/src/components/sidebar/ActivityTab.tsx`** (NEW)

Container that composes stats card, filters, and feed list:

```typescript
import { useAutonomyFeed, type FeedEvent } from '@/hooks/useAutonomyFeed';
import { AutonomyStatsCard } from './AutonomyStatsCard';
import { FeedFilters } from './FeedFilters';
import { ActivityFeedItem } from './ActivityFeedItem';
import { EmptyState } from '@/components/ui/EmptyState';
import { Activity } from 'lucide-react';

interface ActivityTabProps {
  projectId: string | undefined;
  agents: Array<{ id: string; name: string; role: string }>;
}
```

Render order:
1. `<AutonomyStatsCard stats={stats} isLoading={isLoading} />`
2. `<FeedFilters activeFilter={activeFilter} onFilterChange={setActiveFilter} agentFilter={agentFilter} onAgentFilterChange={setAgentFilter} timeFilter={timeFilter} onTimeFilterChange={setTimeFilter} agents={agents} />`
3. Feed list or empty state:
   - If `events.length === 0 && !isLoading`: render `<EmptyState icon={Activity} title="Your team is ready" description="When your Hatches start working autonomously, you'll see their progress here. Try asking one to work on something in the background." />`
   - If `isLoading`: render 3 skeleton feed item placeholders (shimmer divs: `h-14 rounded-lg bg-[var(--hatchin-surface-hover)] animate-shimmer mb-2`)
   - Otherwise: `events.map(event => <ActivityFeedItem key={event.id} event={event} />)`
4. Feed list wrapper: `<div className="flex-1 overflow-y-auto hide-scrollbar space-y-0.5">`

Export `ActivityTab`.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/sidebar/ActivityTab.tsx contains `export function ActivityTab` or `export default function ActivityTab`
    - client/src/components/sidebar/ActivityTab.tsx contains `import { useAutonomyFeed }`
    - client/src/components/sidebar/ActivityTab.tsx contains `AutonomyStatsCard`
    - client/src/components/sidebar/ActivityTab.tsx contains `FeedFilters`
    - client/src/components/sidebar/ActivityTab.tsx contains `ActivityFeedItem`
    - client/src/components/sidebar/ActivityTab.tsx contains `EmptyState`
    - client/src/components/sidebar/ActivityTab.tsx contains `Your team is ready`
    - client/src/components/sidebar/AutonomyStatsCard.tsx contains `tasksCompleted`
    - client/src/components/sidebar/AutonomyStatsCard.tsx contains `costToday`
    - client/src/components/sidebar/AutonomyStatsCard.tsx contains `animate-shimmer` (loading state)
    - client/src/components/sidebar/FeedFilters.tsx contains `activeFilter`
    - client/src/components/sidebar/FeedFilters.tsx contains `'all'` and `'task'` and `'handoff'` and `'review'` and `'approval'` (filter types)
    - client/src/components/sidebar/FeedFilters.tsx contains `agentFilter`
    - client/src/components/sidebar/FeedFilters.tsx contains `timeFilter`
    - client/src/components/sidebar/ActivityFeedItem.tsx contains `hatchin-green` (task color)
    - client/src/components/sidebar/ActivityFeedItem.tsx contains `hatchin-blue` (handoff color)
    - client/src/components/sidebar/ActivityFeedItem.tsx contains `hatchin-orange` (review color)
    - client/src/components/sidebar/ActivityFeedItem.tsx contains `expandableData`
    - client/src/components/sidebar/ActivityFeedItem.tsx contains `formatRelativeTime`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>ActivityTab renders stats card, filter chips, and feed item list. Stats card shows 3 stat pills with shimmer loading. Filters support event type, agent, and time range. Feed items show agent avatar, color-coded border, label, relative timestamp, and expandable detail. Empty state shows when no events exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add "working" avatar state + wire working agents to LeftSidebar and ProjectTree</name>
  <files>client/src/components/avatars/BaseAvatar.tsx, client/src/index.css, client/src/components/LeftSidebar.tsx, client/src/components/ProjectTree.tsx</files>
  <read_first>
    - client/src/components/avatars/BaseAvatar.tsx (full file — AvatarState type, AvatarWrapper function, existing animation variants, ThinkingBubble component)
    - client/src/index.css (full file — search for @keyframes to find existing animation patterns: ai-thinking-ring, brain-glow-pulse, shimmer, online-pulse)
    - client/src/components/LeftSidebar.tsx (search for "AgentAvatar" or "AvatarState" — how avatars are rendered for agents in the tree)
    - client/src/components/ProjectTree.tsx (search for "AgentAvatar" or "AvatarState" or "avatar" — how agent avatars are rendered in project tree list items)
    - client/src/hooks/useAgentWorkingState.ts (from Plan 01 — hook return type)
    - client/src/components/avatars/AgentAvatar.tsx (how state prop is passed through to individual avatar components)
  </read_first>
  <action>
**File 1: `client/src/components/avatars/BaseAvatar.tsx`** (MODIFY)

1. Extend AvatarState union — change line 9 from:
```typescript
export type AvatarState = "idle" | "thinking" | "speaking" | "celebrating";
```
to:
```typescript
export type AvatarState = "idle" | "thinking" | "speaking" | "celebrating" | "working";
```

2. In the `AvatarWrapper` function, add the "working" ring overlay. Find the return JSX and add INSIDE the outer wrapper div (after the children, before the closing tag of the main container):
```tsx
{state === "working" && (
  <div
    className="absolute inset-0 rounded-full pointer-events-none"
    style={{
      border: '2px dashed rgba(108,130,255,0.6)',
      animation: 'agent-working-ring 3s linear infinite',
      boxShadow: '0 0 8px rgba(108,130,255,0.3)',
    }}
  />
)}
```

Note: The `rgba(108,130,255,...)` matches hatchin-blue/60. The dashed border creates a rotating ring effect. The box-shadow adds a subtle glow.

3. Ensure the outer wrapper div has `position: relative` (it likely already does via Framer Motion styling, but verify). If not, add `className="relative"` to the outer container.

**File 2: `client/src/index.css`** (MODIFY)

Add new keyframe animation. Place it near the existing `@keyframes ai-thinking-ring` block:

```css
@keyframes agent-working-ring {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
```

**File 3: `client/src/components/LeftSidebar.tsx`** (MODIFY)

1. Add import at top:
```typescript
import { useAgentWorkingState } from '@/hooks/useAgentWorkingState';
```

2. Inside the component function, call the hook:
```typescript
const workingAgents = useAgentWorkingState();
```

3. Find where agent avatars are rendered (search for `AgentAvatar` or `state=` prop on avatar components). For each agent avatar, modify the `state` prop:
- Current: `state="idle"` or `state={someState}`
- New: `state={workingAgents.has(agent.id) ? "working" : existingState}`

Where `existingState` is whatever the current state logic is (idle, thinking, etc.). The working state takes priority — if the agent is executing background work, show the working ring regardless of other states.

**File 4: `client/src/components/ProjectTree.tsx`** (MODIFY)

Same pattern as LeftSidebar:

1. Add import:
```typescript
import { useAgentWorkingState } from '@/hooks/useAgentWorkingState';
```

2. Call hook:
```typescript
const workingAgents = useAgentWorkingState();
```

3. Find agent avatar renderings and modify state prop:
```typescript
state={workingAgents.has(agent.id) ? "working" : existingState}
```

Search for all occurrences of `<AgentAvatar` in the file and update each one.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - client/src/components/avatars/BaseAvatar.tsx contains `"working"` in the AvatarState type definition
    - client/src/components/avatars/BaseAvatar.tsx contains `state === "working"` (ring overlay conditional)
    - client/src/components/avatars/BaseAvatar.tsx contains `agent-working-ring` (animation reference)
    - client/src/components/avatars/BaseAvatar.tsx contains `dashed` (dashed border style)
    - client/src/index.css contains `@keyframes agent-working-ring`
    - client/src/index.css contains `rotate(360deg)` inside agent-working-ring
    - client/src/components/LeftSidebar.tsx contains `import { useAgentWorkingState }`
    - client/src/components/LeftSidebar.tsx contains `workingAgents`
    - client/src/components/ProjectTree.tsx contains `import { useAgentWorkingState }`
    - client/src/components/ProjectTree.tsx contains `workingAgents`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>AvatarState extended with "working" state. Working agents show a 3-second rotating dashed ring with subtle glow. LeftSidebar and ProjectTree read from useAgentWorkingState hook and set avatar state to "working" when agents are executing background tasks. CSS keyframe animation added to index.css.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All 4 new sidebar components exist in `client/src/components/sidebar/`
- `grep "working" client/src/components/avatars/BaseAvatar.tsx` shows the type extension and overlay
- `grep "agent-working-ring" client/src/index.css` shows the keyframe
- `grep "useAgentWorkingState" client/src/components/LeftSidebar.tsx client/src/components/ProjectTree.tsx` shows hook usage in both files
- `grep "useAutonomyFeed" client/src/components/sidebar/ActivityTab.tsx` shows hook usage
</verification>

<success_criteria>
- Activity tab shows stats card, filter chips, and feed items (or empty state)
- Stats card displays 3 pills: tasks completed, handoffs, cost spent today
- Filter chips work for event type (5 options), agent (dropdown), and time (3 options)
- Feed items display with color-coded left border, agent avatar, label, relative timestamp
- Feed items expand on click to show risk score, peer review outcome, handoff context
- New events animate in with opacity + translateY entrance
- Agent avatars in LeftSidebar and ProjectTree show rotating dashed ring when working
</success_criteria>

<output>
After completion, create `.planning/phases/11-sidebar-shell-activity-feed/11-03-SUMMARY.md`
</output>
