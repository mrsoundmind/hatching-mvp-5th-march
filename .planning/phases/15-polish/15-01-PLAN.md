---
phase: 15-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/sidebar/SidebarTabBar.tsx
  - client/src/components/sidebar/ApprovalsEmptyState.tsx
  - client/src/components/sidebar/AutonomyStatsCard.tsx
autonomous: true
requirements: [PLSH-01]

must_haves:
  truths:
    - "SidebarTabBar buttons have 44px minimum touch targets on mobile viewports"
    - "Empty state icons float with gentle bobbing animation"
    - "Stats card numbers animate in on mount"
    - "All three components use consistent spacing on 4px grid"
  artifacts:
    - path: "client/src/components/sidebar/SidebarTabBar.tsx"
      provides: "Mobile-compliant tab bar with 44px touch targets"
      contains: "min-h-[44px]"
    - path: "client/src/components/sidebar/ApprovalsEmptyState.tsx"
      provides: "Animated empty state with floating icon"
      contains: "animate"
    - path: "client/src/components/sidebar/AutonomyStatsCard.tsx"
      provides: "Premium card with animated stat numbers"
      contains: "premium-card"
  key_links:
    - from: "SidebarTabBar.tsx"
      to: "RightSidebar.tsx"
      via: "tab switching"
      pattern: "min-h-\\[44px\\]"
---

<objective>
Polish the three always-visible top-level sidebar components: tab bar, empty states, and stats card.

Purpose: These are the first things users see in the right sidebar. Polishing them first establishes the premium baseline for all subsequent component work.
Output: Three components with mobile touch targets, animated empty states, and premium card treatment on stats.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-polish/15-CONTEXT.md
@.planning/phases/15-polish/15-RESEARCH.md

@client/src/components/sidebar/SidebarTabBar.tsx
@client/src/components/sidebar/ApprovalsEmptyState.tsx
@client/src/components/sidebar/AutonomyStatsCard.tsx
@client/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish SidebarTabBar — mobile touch targets + spacing</name>
  <files>client/src/components/sidebar/SidebarTabBar.tsx</files>
  <read_first>
    - client/src/components/sidebar/SidebarTabBar.tsx
    - client/src/index.css
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Polish SidebarTabBar with these specific changes:

1. **Mobile touch targets**: Add `min-h-[44px] lg:min-h-auto` to each tab button element. This ensures 44px tap targets on mobile (Apple HIG) without affecting desktop layout.

2. **Spacing grid**: Verify all padding/margin/gap values snap to 4px increments (4, 8, 12, 16, 20, 24). If any values like `py-1.5` (6px) or `gap-1.5` (6px) exist, round to nearest 4px increment (`py-2` = 8px or `py-1` = 4px).

3. **Badge consistency**: Ensure badge/pill elements use the unified pill pattern: `text-[10px] font-semibold px-2 py-0.5 rounded-full`. Color varies by tab meaning but shape is consistent.

Do NOT change the layoutId, spring animation config, or tab switching logic — only visual refinement.

After changes, run `npx tsc --noEmit --pretty` to verify no type errors. Then use Playwright MCP to take a screenshot of the sidebar at desktop width AND at 375px mobile width to visually verify the tab bar.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SidebarTabBar.tsx contains `min-h-[44px]` string (mobile touch target)
    - SidebarTabBar.tsx contains `lg:min-h-auto` string (desktop override)
    - All spacing values in the file are multiples of 4px (no py-1.5, gap-1.5, etc.)
    - TypeScript typecheck passes with zero errors
    - Playwright screenshot taken at 375px width shows tab bar with visible, tappable buttons
  </acceptance_criteria>
  <done>Tab bar has 44px mobile touch targets, consistent spacing, and passes typecheck</done>
</task>

<task type="auto">
  <name>Task 2: Polish ApprovalsEmptyState — floating icon animation + fade-in text</name>
  <files>client/src/components/sidebar/ApprovalsEmptyState.tsx</files>
  <read_first>
    - client/src/components/sidebar/ApprovalsEmptyState.tsx
    - client/src/index.css
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Polish ApprovalsEmptyState (which wraps the shared EmptyState pattern) with these specific changes:

1. **Floating icon animation**: Wrap the icon container in a `motion.div` from framer-motion with:
   ```
   animate={{ y: [0, -4, 0] }}
   transition={{ repeat: Infinity, duration: 3, ease: 'easeInOut' }}
   ```
   This creates a gentle floating/bobbing effect on the empty state icon. The icon container should have `bg-[var(--hatchin-surface)]` background and rounded-2xl shape.

2. **Fade-in text**: Wrap the title `h3` and description `p` elements in `motion` elements with:
   - Title: `initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.15 }}`
   - Description: `initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.3 }}`

3. **Spacing**: Ensure all padding/margin values snap to 4px grid (4, 8, 12, 16, 20, 24).

Import `motion` from `framer-motion` if not already imported.

After changes, run typecheck and take a Playwright screenshot of the Approvals tab in empty state to verify the floating animation is visible.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ApprovalsEmptyState.tsx contains `motion` import from `framer-motion`
    - ApprovalsEmptyState.tsx contains `animate={{ y: [0, -4, 0] }}` or equivalent floating animation
    - ApprovalsEmptyState.tsx contains `delay: 0.15` for staggered fade-in
    - TypeScript typecheck passes with zero errors
    - Playwright screenshot shows the empty state with centered icon and text
  </acceptance_criteria>
  <done>Empty state has floating icon bobbing animation and staggered fade-in text</done>
</task>

<task type="auto">
  <name>Task 3: Polish AutonomyStatsCard — premium card treatment + number entry animation</name>
  <files>client/src/components/sidebar/AutonomyStatsCard.tsx</files>
  <read_first>
    - client/src/components/sidebar/AutonomyStatsCard.tsx
    - client/src/index.css
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Polish AutonomyStatsCard with these specific changes:

1. **Premium card class**: Replace the outermost container's background/border classes with the `premium-card` CSS class (defined in index.css ~line 600). This applies: glass background, backdrop-filter blur, consistent border, box-shadow, and border-radius: 12px.

2. **Number entry animation**: Wrap each stat number value in a `motion.span` with:
   ```
   initial={{ opacity: 0, y: 4 }}
   animate={{ opacity: 1, y: 0 }}
   transition={{ duration: 0.3, delay: index * 0.1 }}
   ```
   Where `index` is 0, 1, 2 for each stat (tasks completed, handoffs, cost). This creates a staggered reveal effect.

3. **Hover lift**: Add `whileHover={{ y: -1 }}` to the outermost `motion.div` container with `transition={{ type: 'spring', stiffness: 400, damping: 25 }}`.

4. **Entry animation**: Add standard Framer entry: `initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.18, ease: 'easeOut' }}` to the card container.

5. **Spacing**: Snap all padding/margin/gap to 4px grid.

After changes, run typecheck and take a Playwright screenshot of the Activity tab showing the stats card.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AutonomyStatsCard.tsx contains `premium-card` class string
    - AutonomyStatsCard.tsx contains `motion.span` for animated numbers
    - AutonomyStatsCard.tsx contains `whileHover` for hover lift
    - AutonomyStatsCard.tsx contains `initial={{ opacity: 0` for entry animation
    - TypeScript typecheck passes with zero errors
    - Playwright screenshot shows stats card with visible card treatment at top of Activity tab
  </acceptance_criteria>
  <done>Stats card has premium glass treatment, animated number reveal, and hover lift</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `npx tsc --noEmit --pretty` passes with zero errors
2. Playwright screenshots of: (a) tab bar at 375px, (b) Activity tab with stats card, (c) Approvals tab empty state
3. All components use 4px spacing grid
</verification>

<success_criteria>
- Tab bar has mobile-compliant 44px touch targets
- Empty state icon floats with 3s bobbing animation
- Stats card uses premium-card class with animated numbers
- All changes pass TypeScript strict mode
- Visual verification via Playwright confirms no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/15-polish/15-01-SUMMARY.md`
</output>
