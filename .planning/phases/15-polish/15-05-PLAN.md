---
phase: 15-polish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/sidebar/WorkOutputSection.tsx
  - client/src/components/sidebar/BrainDocsTab.tsx
  - client/src/components/sidebar/ActivityTab.tsx
autonomous: true
requirements: [PLSH-01]

must_haves:
  truths:
    - "Work output uses prose rendering instead of raw pre/monospace"
    - "BrainDocsTab has section dividers between Upload, Documents, Settings, and Work Output sections"
    - "ActivityTab loading skeleton uses shimmer animation"
  artifacts:
    - path: "client/src/components/sidebar/WorkOutputSection.tsx"
      provides: "Prose-styled output rendering with agent avatar"
      contains: "prose"
    - path: "client/src/components/sidebar/BrainDocsTab.tsx"
      provides: "Section dividers between content areas"
      contains: "bg-gradient-to-r"
    - path: "client/src/components/sidebar/ActivityTab.tsx"
      provides: "Shimmer loading skeleton"
      contains: "shimmer"
  key_links:
    - from: "WorkOutputSection.tsx"
      to: "BrainDocsTab.tsx"
      via: "rendered in work output area"
      pattern: "prose"
    - from: "BrainDocsTab.tsx"
      to: "RightSidebar.tsx"
      via: "rendered in Brain & Docs tab"
      pattern: "bg-gradient-to-r"
---

<objective>
Polish the remaining content components: work output rendering, Brain tab section dividers, and Activity tab loading state.

Purpose: WorkOutputSection currently renders in raw monospace pre tags (developer-y feel), BrainDocsTab needs visual separation between its 4 sub-sections, and ActivityTab needs a proper shimmer loading skeleton.
Output: Three components with polished rendering, clear visual hierarchy, and premium loading states.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-polish/15-CONTEXT.md
@.planning/phases/15-polish/15-RESEARCH.md

@client/src/components/sidebar/WorkOutputSection.tsx
@client/src/components/sidebar/BrainDocsTab.tsx
@client/src/components/sidebar/ActivityTab.tsx
@client/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish WorkOutputSection — prose rendering + agent avatar + section divider</name>
  <files>client/src/components/sidebar/WorkOutputSection.tsx</files>
  <read_first>
    - client/src/components/sidebar/WorkOutputSection.tsx
    - client/src/index.css
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Polish WorkOutputSection with these specific changes:

1. **Prose rendering instead of pre**: Replace the `<pre>` element that renders agent output with a `<div className="prose prose-sm max-w-none">` container. The `@tailwindcss/typography` plugin is already installed. This renders the output with proper paragraph spacing, line heights, and reading-friendly typography instead of monospace. Keep `whitespace-pre-wrap` if the content needs to preserve line breaks.

2. **Agent avatar**: If the component shows an agent initial letter in a colored circle (e.g., `<div className="w-6 h-6 rounded-full bg-...">A</div>`), replace it with the actual `AgentAvatar` component at 24px size:
   ```tsx
   import { AgentAvatar } from '@/components/avatars/AgentAvatar';
   // ...
   <AgentAvatar agent={agent} size={24} />
   ```
   If no agent prop is available, keep the letter circle but ensure it uses `bg-[var(--hatchin-blue)]` consistently.

3. **Section header divider**: Apply the unified section header pattern:
   ```tsx
   <div className="flex items-center gap-2 mb-3">
     <span className="text-[11px] font-semibold text-[var(--hatchin-text-muted)] uppercase tracking-wider">
       Work Outputs
     </span>
     <div className="flex-1 h-px bg-gradient-to-r from-[var(--hatchin-border-subtle)] to-transparent" />
   </div>
   ```

4. **Card treatment**: Apply `premium-card` class to each output card container for consistency with other sidebar cards.

5. **Spacing**: Snap all padding/margin/gap to 4px grid.

After changes, run typecheck and take a Playwright screenshot of the work output section in the Brain tab.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - WorkOutputSection.tsx contains `prose` class for typography rendering
    - WorkOutputSection.tsx contains `bg-gradient-to-r` for section header divider OR `uppercase tracking-wider` for section header
    - WorkOutputSection.tsx does NOT render output in a `<pre>` tag with monospace font as the primary display
    - TypeScript typecheck passes with zero errors
  </acceptance_criteria>
  <done>Work output uses prose typography, has agent avatar, and section header divider</done>
</task>

<task type="auto">
  <name>Task 2: Polish BrainDocsTab — section dividers between sub-sections + spacing</name>
  <files>client/src/components/sidebar/BrainDocsTab.tsx</files>
  <read_first>
    - client/src/components/sidebar/BrainDocsTab.tsx
    - client/src/components/sidebar/AutonomySettingsPanel.tsx
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Polish BrainDocsTab with these specific changes:

1. **Section dividers**: BrainDocsTab renders 4 sub-sections: Upload Zone, Document List, Autonomy Settings, and Work Outputs. Add visual separation between each section using the unified section header pattern. For sections that already have labels/titles, replace with:
   ```tsx
   <div className="flex items-center gap-2 mb-3 mt-4">
     <span className="text-[11px] font-semibold text-[var(--hatchin-text-muted)] uppercase tracking-wider">
       {sectionLabel}
     </span>
     <div className="flex-1 h-px bg-gradient-to-r from-[var(--hatchin-border-subtle)] to-transparent" />
   </div>
   ```
   Section labels: "Upload", "Knowledge Base", "Autonomy Settings", "Work Outputs" (or whatever labels the component currently uses — preserve them but apply the divider pattern).

2. **Consistent vertical rhythm**: Ensure each major section has `mt-4` (16px) top margin and `mb-3` (12px) bottom margin on its header. This creates a consistent rhythm between sections.

3. **Spacing**: Snap all padding/margin/gap to 4px grid. The tab's outer padding should be `p-4` (16px).

4. **Scroll container**: Ensure the tab content area has `overflow-y-auto` and proper scrollbar styling if not already present.

After changes, run typecheck and take a Playwright screenshot showing the full Brain tab with all 4 sections visible.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - BrainDocsTab.tsx contains at least 2 instances of `bg-gradient-to-r` (section dividers for multiple sub-sections)
    - BrainDocsTab.tsx contains `uppercase tracking-wider` for section headers
    - BrainDocsTab.tsx contains `from-[var(--hatchin-border-subtle)]` in divider gradients
    - TypeScript typecheck passes with zero errors
    - Playwright screenshot shows clear visual separation between Upload, Documents, Settings, and Work Output sections
  </acceptance_criteria>
  <done>Brain tab has consistent section dividers, vertical rhythm, and 4px spacing grid</done>
</task>

<task type="auto">
  <name>Task 3: Polish ActivityTab — loading skeleton shimmer + spacing</name>
  <files>client/src/components/sidebar/ActivityTab.tsx</files>
  <read_first>
    - client/src/components/sidebar/ActivityTab.tsx
    - client/src/index.css
    - tailwind.config.ts
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Polish ActivityTab with these specific changes:

1. **Shimmer loading skeleton**: If the Activity tab has a loading state that uses plain gray rectangles or a spinner, replace with content-shaped skeleton placeholders using the shimmer animation. The `@keyframes shimmer` exists in index.css. Create skeleton placeholders that match the shape of the actual content:
   ```tsx
   // Skeleton for stats card
   <div className="rounded-xl h-20 bg-[var(--hatchin-surface)] overflow-hidden">
     <div className="w-full h-full" style={{ animation: 'shimmer 1.5s ease-in-out infinite' }} />
   </div>
   // Skeleton for feed items (3 placeholder rows)
   {[1, 2, 3].map(i => (
     <div key={i} className="rounded-xl h-16 bg-[var(--hatchin-surface)] overflow-hidden">
       <div className="w-full h-full" style={{ animation: 'shimmer 1.5s ease-in-out infinite', animationDelay: `${i * 0.15}s` }} />
     </div>
   ))}
   ```
   Note: If `animate-shimmer` Tailwind class does not work, use inline `style={{ animation: 'shimmer 1.5s ease-in-out infinite' }}` instead.

2. **Check shimmer in tailwind.config**: Read `tailwind.config.ts`. If the `animation` key does NOT include `shimmer`, add it:
   ```
   animation: {
     shimmer: 'shimmer 1.5s ease-in-out infinite',
     // ... existing animations
   }
   ```
   This enables `animate-shimmer` as a Tailwind utility class.

3. **Spacing**: Snap all padding/margin/gap to 4px grid.

After changes, run typecheck. To verify shimmer, take a Playwright screenshot while the tab is in loading state (or briefly before data loads).
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ActivityTab.tsx contains `shimmer` string (either as class or animation reference)
    - ActivityTab.tsx contains skeleton placeholder elements with rounded corners (`rounded-xl` or `rounded-lg`)
    - TypeScript typecheck passes with zero errors
  </acceptance_criteria>
  <done>Activity tab has content-shaped shimmer loading skeletons and consistent spacing</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `npx tsc --noEmit --pretty` passes with zero errors
2. Playwright screenshots of: (a) Work output with prose rendering, (b) Brain tab with section dividers, (c) Activity tab loading state
3. No raw `<pre>` tags with monospace rendering for user-facing output content
</verification>

<success_criteria>
- Work output renders in prose typography, not monospace
- Brain tab has clear section dividers with gradient lines
- Activity tab has shimmer loading skeletons
- All changes pass TypeScript strict mode
</success_criteria>

<output>
After completion, create `.planning/phases/15-polish/15-05-SUMMARY.md`
</output>
