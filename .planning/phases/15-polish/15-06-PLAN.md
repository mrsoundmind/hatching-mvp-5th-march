---
phase: 15-polish
plan: 06
type: execute
wave: 2
depends_on: [15-01, 15-02, 15-03, 15-04, 15-05]
files_modified:
  - client/src/index.css
  - client/src/components/AutonomousApprovalCard.tsx
  - client/src/pages/home.tsx
autonomous: false
requirements: [PLSH-01]

must_haves:
  truths:
    - "Avatar working animation has dual-ring effect (pulsing outer + rotating inner)"
    - "AutonomousApprovalCard uses CSS variable pattern consistently"
    - "Sheet drawer has visible grab handle and frosted glass backdrop"
    - "All polished components are visually consistent across dark and light modes"
  artifacts:
    - path: "client/src/index.css"
      provides: "New avatar-working-pulse keyframe animation"
      contains: "avatar-working-pulse"
    - path: "client/src/components/AutonomousApprovalCard.tsx"
      provides: "Normalized CSS variable references"
      contains: "var(--hatchin-blue)"
    - path: "client/src/pages/home.tsx"
      provides: "Sheet drawer with grab handle and backdrop blur"
      contains: "backdrop-blur"
  key_links:
    - from: "index.css"
      to: "BaseAvatar or avatar components"
      via: "CSS animation reference"
      pattern: "avatar-working-pulse"
    - from: "home.tsx"
      to: "RightSidebar.tsx"
      via: "Sheet drawer wrapping sidebar on mobile"
      pattern: "backdrop-blur"
---

<objective>
Final polish pass: avatar working animation enhancement, AutonomousApprovalCard normalization, Sheet drawer upgrade, and full visual audit checkpoint.

Purpose: This is the cross-cutting polish plan that touches the avatar animation (index.css), the chat-level approval card, and the mobile Sheet drawer. Ends with a human-verify checkpoint to confirm the entire sidebar feels premium and unified.
Output: Enhanced avatar glow, normalized card, frosted glass mobile drawer, and visual sign-off.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-polish/15-CONTEXT.md
@.planning/phases/15-polish/15-RESEARCH.md

@client/src/index.css
@client/src/components/AutonomousApprovalCard.tsx
@client/src/pages/home.tsx
@client/src/components/RightSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance avatar working animation — dual ring with glow pulse</name>
  <files>client/src/index.css</files>
  <read_first>
    - client/src/index.css
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Enhance the avatar working state animation with a dual-ring effect:

1. **Add new keyframe to index.css**: Add the `avatar-working-pulse` keyframe near the existing `agent-working-ring` keyframe (~line 567):
   ```css
   @keyframes avatar-working-pulse {
     0%, 100% {
       transform: scale(1);
       opacity: 0.4;
     }
     50% {
       transform: scale(1.18);
       opacity: 0;
     }
   }
   ```

2. **Add a CSS class for the pulse ring**: Add a utility class that can be applied to the avatar's working state overlay:
   ```css
   .avatar-working-pulse-ring {
     animation: avatar-working-pulse 2s ease-out infinite;
     border: 2px solid rgba(108, 130, 255, 0.4);
     border-radius: 9999px;
     position: absolute;
     inset: -2px;
     pointer-events: none;
   }
   ```

3. **Enhance existing working ring**: Find the existing `agent-working-ring` animation class and add a subtle `box-shadow: 0 0 8px rgba(108, 130, 255, 0.3)` to the dashed ring element's styling, giving it a glow effect.

4. **Find where the working state ring is rendered** (likely in a BaseAvatar or avatar component). Add the pulse ring as a sibling element behind the existing dashed ring:
   - The pulse ring should use the `avatar-working-pulse-ring` class
   - The existing dashed ring stays unchanged but gains the box-shadow glow
   - Together they create a dual-ring effect: pulsing outer glow + rotating inner dashed ring

After changes, run typecheck and take a Playwright screenshot. Note: the animation won't be visible in a static screenshot — the typecheck and visual layout verification are sufficient.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - index.css contains `@keyframes avatar-working-pulse` definition
    - index.css contains `avatar-working-pulse-ring` class
    - index.css contains `box-shadow:` rule with `rgba(108, 130, 255` near the working ring styles
    - TypeScript typecheck passes with zero errors
  </acceptance_criteria>
  <done>Avatar working state has dual-ring animation: pulsing outer glow + rotating inner dashed ring</done>
</task>

<task type="auto">
  <name>Task 2: Normalize AutonomousApprovalCard + Sheet drawer polish</name>
  <files>client/src/components/AutonomousApprovalCard.tsx, client/src/pages/home.tsx</files>
  <read_first>
    - client/src/components/AutonomousApprovalCard.tsx
    - client/src/pages/home.tsx
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Two components in one task (both are small, targeted changes):

**A. AutonomousApprovalCard normalization:**

1. **Normalize color references**: Replace any Tailwind color classes that bypass CSS variables with the CSS variable pattern:
   - `text-hatchin-text-bright` to `text-[var(--hatchin-text-bright)]`
   - `bg-hatchin-blue` to `bg-[var(--hatchin-blue)]`
   - `border-hatchin-border` to `border-[var(--hatchin-border)]`
   Search for any bare `hatchin-` Tailwind utilities (without the `var(--` wrapper) and normalize them.

2. **Mobile touch targets**: Add `min-h-[44px] lg:min-h-auto` to the Approve and Reject buttons.

3. **Premium card**: Apply `premium-card` class to the card container if not already using it.

**B. Sheet drawer polish (home.tsx):**

1. **Grab handle**: Inside the right-side `SheetContent`, add a visible grab handle bar at the top:
   ```tsx
   <div className="w-10 h-1 bg-[var(--hatchin-border)] rounded-full mx-auto mt-3 mb-0 shrink-0" />
   ```
   Place this as the first child inside `SheetContent`, before the sidebar content.

2. **Frosted glass backdrop**: Add `backdrop-blur-xl` and `bg-[var(--glass-frosted-strong)]` to the `SheetContent` className. Apply to BOTH left and right Sheet drawers for consistency. The Shadcn Sheet's `className` prop passes through to the panel element.

3. **Also add grab handle to the left sidebar Sheet** for consistency.

After changes, run typecheck and take Playwright screenshots at 375px mobile width showing both the left and right Sheet drawers open.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AutonomousApprovalCard.tsx contains `var(--hatchin-blue)` (normalized, not bare `hatchin-blue`)
    - AutonomousApprovalCard.tsx contains `min-h-[44px]` on action buttons
    - home.tsx contains `backdrop-blur` on SheetContent elements
    - home.tsx contains `w-10 h-1` grab handle div inside Sheet drawers
    - home.tsx contains `bg-[var(--glass-frosted-strong)]` or equivalent frosted background on Sheet
    - TypeScript typecheck passes with zero errors
    - Playwright screenshot at 375px shows frosted glass Sheet drawer with visible grab handle
  </acceptance_criteria>
  <done>Approval card normalized to CSS variables, Sheet drawers have grab handle and frosted glass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual audit checkpoint — full sidebar polish verification</name>
  <files>client/src/components/sidebar/, client/src/pages/home.tsx</files>
  <action>
Human verifies the complete visual polish pass across all sidebar tabs, mobile drawers, and dark/light modes.

Verification steps:
1. Start dev server: `npm run dev` (port 5001)
2. Log in and navigate to a project
3. Activity tab: Verify stats card has glass card treatment, feed items have hover lift, filter chips are tappable on mobile
4. Brain and Docs tab: Verify section dividers between Upload/Docs/Settings/Outputs, upload zone has dashed border, document cards hover-lift
5. Approvals tab: Verify approval items have glass cards, approve/reject buttons are large on mobile
6. Mobile (resize to 375px or use mobile mode): Verify Sheet drawers have grab handle at top and frosted glass background
7. Toggle dark/light mode: Verify all components look correct in both themes
8. Overall feel: All sidebar components should feel like they belong to one unified design system

Resume signal: Type "approved" if the polish looks premium and consistent, or describe specific issues to fix.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <done>Human approves that all polished components feel premium, consistent, and visually unified across all tabs, modes, and viewport sizes</done>
</task>

</tasks>

<verification>
After all tasks + checkpoint:
1. `npx tsc --noEmit --pretty` passes with zero errors
2. Human confirms visual consistency across all 3 tabs + mobile view + dark/light modes
3. All card components use `premium-card` class
4. All mobile touch targets are 44px minimum
5. No undefined CSS variable references remain
</verification>

<success_criteria>
- Avatar working animation has visible dual-ring glow effect
- AutonomousApprovalCard uses CSS variable pattern consistently
- Sheet drawers have grab handle and frosted glass backdrop
- Human approves overall visual quality and consistency
- All changes pass TypeScript strict mode
</success_criteria>

<output>
After completion, create `.planning/phases/15-polish/15-06-SUMMARY.md`
</output>
