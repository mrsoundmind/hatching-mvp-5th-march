---
phase: 15-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/sidebar/ActivityFeedItem.tsx
  - client/src/components/sidebar/FeedFilters.tsx
  - client/src/components/sidebar/HandoffChainTimeline.tsx
autonomous: true
requirements: [PLSH-01]

must_haves:
  truths:
    - "Activity feed items use unified premium-card treatment with hover lift"
    - "Feed filters use Shadcn Select component instead of raw HTML select"
    - "Handoff chain timeline nodes have staggered entry animation"
    - "All filter chips have 44px mobile touch targets"
  artifacts:
    - path: "client/src/components/sidebar/ActivityFeedItem.tsx"
      provides: "Premium card feed items with hover lift"
      contains: "premium-card"
    - path: "client/src/components/sidebar/FeedFilters.tsx"
      provides: "Shadcn Select for agent filter, mobile-compliant chips"
      contains: "SelectTrigger"
    - path: "client/src/components/sidebar/HandoffChainTimeline.tsx"
      provides: "Staggered entry animation on chain nodes"
      contains: "delay"
  key_links:
    - from: "ActivityFeedItem.tsx"
      to: "ActivityTab.tsx"
      via: "rendered in feed list"
      pattern: "premium-card"
    - from: "FeedFilters.tsx"
      to: "ActivityTab.tsx"
      via: "filter state management"
      pattern: "SelectTrigger"
---

<objective>
Polish the Activity tab content components: feed items, filters, and handoff chain timeline.

Purpose: These are the primary content components in the Activity tab — the most-used tab in the sidebar. Feed items need unified card treatment, the raw HTML select needs replacement with Shadcn Select, and the handoff timeline needs entry animation.
Output: Three components with premium card style, design-system-consistent dropdowns, and animated timeline.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-polish/15-CONTEXT.md
@.planning/phases/15-polish/15-RESEARCH.md

@client/src/components/sidebar/ActivityFeedItem.tsx
@client/src/components/sidebar/FeedFilters.tsx
@client/src/components/sidebar/HandoffChainTimeline.tsx
@client/src/components/sidebar/AutonomySettingsPanel.tsx
@client/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish ActivityFeedItem — premium card + hover lift + consistent badges</name>
  <files>client/src/components/sidebar/ActivityFeedItem.tsx</files>
  <read_first>
    - client/src/components/sidebar/ActivityFeedItem.tsx
    - client/src/index.css
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Polish ActivityFeedItem with these specific changes:

1. **Unified card treatment**: Replace the existing background classes (likely `hover:bg-[var(--hatchin-surface-hover)]` or similar) with the `premium-card` CSS class on the outermost container div. This applies glass background, backdrop-filter blur, border, shadow, and 12px border-radius.

2. **Hover lift**: If the container is already a `motion.div`, add `whileHover={{ y: -1 }}`. If it's a plain `div`, convert to `motion.div` (import `motion` from `framer-motion`) and add:
   ```
   whileHover={{ y: -1 }}
   transition={{ type: 'spring', stiffness: 400, damping: 25 }}
   ```

3. **Entry animation**: Add standard entry if not present:
   ```
   initial={{ opacity: 0, y: 8 }}
   animate={{ opacity: 1, y: 0 }}
   transition={{ duration: 0.18, ease: 'easeOut' }}
   ```

4. **Badge consistency**: Ensure any type/status badges use the unified pill pattern: `text-[10px] font-semibold px-2 py-0.5 rounded-full`. Keep existing category color coding (task=green, handoff=blue, review=orange, approval=amber).

5. **Fix undefined CSS variable**: If `var(--hatchin-surface-hover)` is used anywhere, replace with `var(--hatchin-surface-elevated)` which is actually defined in index.css.

6. **Spacing**: Snap all padding/margin/gap to 4px grid increments.

After changes, run typecheck and take a Playwright screenshot of the Activity tab with feed items visible.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ActivityFeedItem.tsx contains `premium-card` class
    - ActivityFeedItem.tsx contains `whileHover` for hover lift
    - ActivityFeedItem.tsx does NOT contain `--hatchin-surface-hover` (undefined variable removed)
    - ActivityFeedItem.tsx badge elements contain `rounded-full` and `text-[10px]`
    - TypeScript typecheck passes with zero errors
    - Playwright screenshot shows feed items with visible card borders and glass effect
  </acceptance_criteria>
  <done>Feed items use premium-card with hover lift, consistent badges, no undefined CSS variables</done>
</task>

<task type="auto">
  <name>Task 2: Polish FeedFilters — Shadcn Select + mobile touch targets</name>
  <files>client/src/components/sidebar/FeedFilters.tsx</files>
  <read_first>
    - client/src/components/sidebar/FeedFilters.tsx
    - client/src/components/sidebar/AutonomySettingsPanel.tsx
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Polish FeedFilters with these specific changes:

1. **Replace raw HTML select with Shadcn Select**: The agent filter currently uses a raw `<select>` element. Replace it with the Shadcn Select component pattern (reference: AutonomySettingsPanel.tsx already uses this exact pattern):
   ```typescript
   import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';

   <Select value={agentFilter ?? ''} onValueChange={(v) => onAgentFilterChange(v || null)}>
     <SelectTrigger className="h-8 text-[11px] rounded-full border-[var(--hatchin-border-subtle)]">
       <SelectValue placeholder="All agents" />
     </SelectTrigger>
     <SelectContent>
       <SelectItem value="" className="text-xs">All agents</SelectItem>
       {agents.map(agent => (
         <SelectItem key={agent.id} value={agent.id} className="text-xs">{agent.name}</SelectItem>
       ))}
     </SelectContent>
   </Select>
   ```
   Adapt the value/onChange to match the existing prop interface.

2. **Mobile touch targets**: Add `min-h-[44px] lg:min-h-auto` to ALL filter chip buttons. This ensures 44px tap targets on mobile without affecting desktop.

3. **Unified pill style on chips**: Ensure filter chips use `text-[10px] font-semibold px-2 py-0.5 rounded-full` base style. Active state uses filled background, inactive uses outlined.

4. **Spacing**: Snap all padding/margin/gap to 4px grid.

After changes, run typecheck and take Playwright screenshots at both desktop and 375px mobile width to verify the Select dropdown and chip sizes.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - FeedFilters.tsx contains `SelectTrigger` import from Shadcn (not raw `<select>`)
    - FeedFilters.tsx contains `min-h-[44px]` for mobile touch targets
    - FeedFilters.tsx contains `lg:min-h-auto` for desktop override
    - FeedFilters.tsx does NOT contain a raw `<select>` HTML element
    - TypeScript typecheck passes with zero errors
    - Playwright screenshot at 375px shows visibly larger tap targets on filter chips
  </acceptance_criteria>
  <done>Agent filter uses Shadcn Select, all chips have 44px mobile touch targets</done>
</task>

<task type="auto">
  <name>Task 3: Polish HandoffChainTimeline — staggered node entry + enhanced connector</name>
  <files>client/src/components/sidebar/HandoffChainTimeline.tsx</files>
  <read_first>
    - client/src/components/sidebar/HandoffChainTimeline.tsx
    - client/src/components/sidebar/handoffChainUtils.ts
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Polish HandoffChainTimeline with these specific changes:

1. **Staggered entry animation**: Wrap each chain node row in a `motion.div` with staggered delay:
   ```
   initial={{ opacity: 0, x: -8 }}
   animate={{ opacity: 1, x: 0 }}
   transition={{ delay: index * 0.06, duration: 0.18, ease: 'easeOut' }}
   ```
   Where `index` is the position of the node in the chain.

2. **Enhanced connector**: Replace the plain vertical connector div between nodes with a `motion.div` that has a gradient fill:
   ```
   className="w-0.5 bg-gradient-to-b from-[var(--hatchin-blue)] to-[var(--hatchin-blue)]/30"
   ```
   Add entry animation: `initial={{ scaleY: 0 }} animate={{ scaleY: 1 }} transition={{ duration: 0.3, delay: index * 0.06 }}` with `origin-top` transform origin.

3. **Node card treatment**: Apply `premium-card` class to each node card container for consistent glass background.

4. **Spacing**: Snap all padding/margin/gap to 4px grid.

After changes, run typecheck and take a Playwright screenshot showing a handoff chain in the Activity tab sidebar.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - HandoffChainTimeline.tsx contains `delay: index * 0.06` or equivalent staggered delay
    - HandoffChainTimeline.tsx contains `gradient` or `from-[var(--hatchin-blue)]` for connector styling
    - HandoffChainTimeline.tsx contains `motion.div` for animated nodes
    - TypeScript typecheck passes with zero errors
  </acceptance_criteria>
  <done>Handoff chain has staggered node entry, gradient connectors, and premium card nodes</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `npx tsc --noEmit --pretty` passes with zero errors
2. Playwright screenshots of: (a) Activity feed items with premium cards, (b) Filter chips at mobile width, (c) Handoff chain timeline
3. No raw `<select>` elements remain in FeedFilters
4. No references to undefined `--hatchin-surface-hover` CSS variable
</verification>

<success_criteria>
- Feed items use premium-card with hover lift animation
- Agent filter uses Shadcn Select matching design system
- All filter chips have 44px mobile touch targets
- Handoff chain nodes animate in with stagger
- All changes pass TypeScript strict mode
</success_criteria>

<output>
After completion, create `.planning/phases/15-polish/15-02-SUMMARY.md`
</output>
