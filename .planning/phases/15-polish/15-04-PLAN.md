---
phase: 15-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/sidebar/DocumentCard.tsx
  - client/src/components/sidebar/DocumentUploadZone.tsx
  - client/src/components/sidebar/AutonomySettingsPanel.tsx
autonomous: true
requirements: [PLSH-01]

must_haves:
  truths:
    - "Document cards have hover lift and mobile-compliant delete button"
    - "Upload zone uses Tailwind classes instead of inline styles"
    - "Settings panel has section header divider lines"
    - "All three components use 4px spacing grid"
  artifacts:
    - path: "client/src/components/sidebar/DocumentCard.tsx"
      provides: "Premium document cards with hover lift and mobile delete"
      contains: "whileHover"
    - path: "client/src/components/sidebar/DocumentUploadZone.tsx"
      provides: "Tailwind-styled upload zone replacing inline styles"
      contains: "border-dashed"
    - path: "client/src/components/sidebar/AutonomySettingsPanel.tsx"
      provides: "Settings panel with section header divider lines"
      contains: "bg-gradient-to-r"
  key_links:
    - from: "DocumentCard.tsx"
      to: "BrainDocsTab.tsx"
      via: "rendered in document list"
      pattern: "whileHover"
    - from: "AutonomySettingsPanel.tsx"
      to: "BrainDocsTab.tsx"
      via: "rendered in settings section"
      pattern: "bg-gradient-to-r"
---

<objective>
Polish the Brain & Docs tab content components: document cards, upload zone, and autonomy settings.

Purpose: The Brain tab is where users configure their project's knowledge and autonomy. Document cards need hover interaction, the upload zone has inline styles that bypass the design system, and settings need visual section separation.
Output: Three components with consistent card treatment, Tailwind-native styling, and clear visual hierarchy.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-polish/15-CONTEXT.md
@.planning/phases/15-polish/15-RESEARCH.md

@client/src/components/sidebar/DocumentCard.tsx
@client/src/components/sidebar/DocumentUploadZone.tsx
@client/src/components/sidebar/AutonomySettingsPanel.tsx
@client/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish DocumentCard — hover lift + mobile delete target + badge unification</name>
  <files>client/src/components/sidebar/DocumentCard.tsx</files>
  <read_first>
    - client/src/components/sidebar/DocumentCard.tsx
    - client/src/index.css
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Polish DocumentCard with these specific changes:

1. **Hover lift**: The card already has a `motion.div` wrapper with `layout` prop (for deletion animation). Add `whileHover={{ y: -1 }}` to this wrapper. Do NOT add `layoutId` (that conflicts with SidebarTabBar's layoutId).

2. **Mobile delete button**: The delete button currently has small dimensions (~32px / w-8 h-8). Add `min-h-[44px] min-w-[44px] lg:min-h-auto lg:min-w-auto lg:w-8 lg:h-8` to ensure 44px tap target on mobile without affecting desktop size.

3. **Type badge unification**: Ensure the document type badge (PDF, DOCX, MD, TXT) uses the unified pill pattern: `text-[10px] font-semibold px-2 py-0.5 rounded-full`. Color map:
   - PDF: `bg-[var(--hatchin-blue)]/15 text-[var(--hatchin-blue)]`
   - DOCX: `bg-[var(--hatchin-orange)]/15 text-[var(--hatchin-orange)]`
   - MD: `bg-[var(--hatchin-green)]/15 text-[var(--hatchin-green)]`
   - TXT: `bg-[var(--hatchin-text-muted)]/15 text-[var(--hatchin-text-muted)]`

4. **Fix undefined CSS variables**: Replace any `var(--hatchin-surface-hover)` with `var(--hatchin-surface-elevated)`.

5. **Spacing**: Snap all padding/margin/gap to 4px grid.

After changes, run typecheck and take a Playwright screenshot of the Brain tab with documents visible. Also take a 375px mobile screenshot to verify delete button size.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DocumentCard.tsx contains `whileHover` for hover lift
    - DocumentCard.tsx contains `min-h-[44px]` on the delete button
    - DocumentCard.tsx contains `rounded-full` on type badge elements
    - DocumentCard.tsx does NOT contain `--hatchin-surface-hover`
    - TypeScript typecheck passes with zero errors
  </acceptance_criteria>
  <done>Document cards have hover lift, 44px mobile delete target, and unified type badges</done>
</task>

<task type="auto">
  <name>Task 2: Polish DocumentUploadZone — move inline styles to Tailwind</name>
  <files>client/src/components/sidebar/DocumentUploadZone.tsx</files>
  <read_first>
    - client/src/components/sidebar/DocumentUploadZone.tsx
    - client/src/index.css
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Polish DocumentUploadZone with these specific changes:

1. **Replace inline styles with Tailwind classes**: The component currently sets `border`, `background`, `minHeight`, `padding`, and `transition` as inline `style={{}}` props. Replace these with equivalent Tailwind classes:
   - `border: '2px dashed ...'` → `border-2 border-dashed border-[var(--hatchin-border-subtle)]`
   - `background: '...'` → `bg-[var(--hatchin-surface)]` (normal) / `bg-[var(--hatchin-surface-elevated)]` (drag active)
   - `minHeight: '...'` → `min-h-[120px]` (or appropriate value)
   - `padding: '...'` → `p-6` or appropriate Tailwind spacing
   - `transition: '...'` → `transition-colors duration-200`
   - `borderRadius: '...'` → `rounded-xl`

2. **Drag state via data attribute**: Use a `data-dragging` attribute (or a conditional className) to toggle between normal and drag-active states instead of inline style ternaries:
   - Normal: `border-[var(--hatchin-border-subtle)] bg-[var(--hatchin-surface)]`
   - Drag active: `border-[var(--hatchin-blue)] bg-[var(--hatchin-blue)]/5`

3. **Hover state**: Add `hover:border-[var(--hatchin-blue)]/50` for subtle feedback when cursor enters the drop zone.

4. **Spacing**: Ensure all internal spacing (icon, text, file size limit) snaps to 4px grid.

Preserve ALL drag-and-drop functionality (onDragEnter, onDragLeave, onDrop, onDragOver, file input change handler). Only change styling approach, not behavior.

After changes, run typecheck and take a Playwright screenshot of the upload zone in the Brain tab.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DocumentUploadZone.tsx contains `border-dashed` Tailwind class
    - DocumentUploadZone.tsx contains `border-2` Tailwind class
    - DocumentUploadZone.tsx contains `rounded-xl` or `rounded-lg` Tailwind class
    - DocumentUploadZone.tsx has minimal or no `style={{}}` props for layout/colors (inline styles for non-CSS-variable properties are acceptable)
    - TypeScript typecheck passes with zero errors
    - Playwright screenshot shows a visible dashed-border upload zone in the Brain tab
  </acceptance_criteria>
  <done>Upload zone uses Tailwind classes instead of inline styles, preserving all drag-drop functionality</done>
</task>

<task type="auto">
  <name>Task 3: Polish AutonomySettingsPanel — section header dividers + spacing</name>
  <files>client/src/components/sidebar/AutonomySettingsPanel.tsx</files>
  <read_first>
    - client/src/components/sidebar/AutonomySettingsPanel.tsx
    - client/src/index.css
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Polish AutonomySettingsPanel with these specific changes:

1. **Section header divider pattern**: Find the section header element(s) (likely `h3` with `uppercase tracking-wider`). Replace with the unified section header pattern:
   ```tsx
   <div className="flex items-center gap-2 mb-3">
     <span className="text-[11px] font-semibold text-[var(--hatchin-text-muted)] uppercase tracking-wider">
       Autonomy Settings
     </span>
     <div className="flex-1 h-px bg-gradient-to-r from-[var(--hatchin-border-subtle)] to-transparent" />
   </div>
   ```
   This adds a gradient divider line that fades from left to right after the section title.

2. **Mobile touch targets**: Add `min-h-[44px] lg:min-h-auto` to the toggle switch and any clickable dial/button elements so they're reliably tappable on mobile.

3. **Spacing**: Snap all padding/margin/gap to 4px grid increments (4, 8, 12, 16, 20, 24).

4. **Preserve flash-save animation**: The `flash-save` CSS class is used for save feedback — keep it exactly as-is. Do not modify the save animation or the Shadcn Select components that are already correctly styled.

After changes, run typecheck and take a Playwright screenshot of the autonomy settings section in the Brain tab.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AutonomySettingsPanel.tsx contains `bg-gradient-to-r` for the section header divider line
    - AutonomySettingsPanel.tsx contains `from-[var(--hatchin-border-subtle)]` in the divider
    - AutonomySettingsPanel.tsx contains `uppercase tracking-wider` on section header text
    - AutonomySettingsPanel.tsx still contains `flash-save` class (preserved, not removed)
    - TypeScript typecheck passes with zero errors
  </acceptance_criteria>
  <done>Settings panel has gradient section dividers, mobile touch targets, and 4px spacing grid</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `npx tsc --noEmit --pretty` passes with zero errors
2. Playwright screenshots of: (a) Document cards with hover lift, (b) Upload zone with Tailwind styling, (c) Settings panel with divider lines
3. No inline styles remain for layout/color properties in DocumentUploadZone
</verification>

<success_criteria>
- Document cards have hover lift and 44px mobile delete button
- Upload zone uses Tailwind classes instead of inline styles
- Settings panel has gradient section header dividers
- All changes pass TypeScript strict mode
</success_criteria>

<output>
After completion, create `.planning/phases/15-polish/15-04-SUMMARY.md`
</output>
