---
phase: 15-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/sidebar/ApprovalItem.tsx
  - client/src/components/sidebar/TaskPipelineView.tsx
autonomous: true
requirements: [PLSH-01]

must_haves:
  truths:
    - "Approve/reject buttons have 44px minimum touch targets on mobile"
    - "Approval items use premium-card treatment matching feed items"
    - "Pipeline stage dots use design-system category colors"
    - "Pipeline rows have hover state"
  artifacts:
    - path: "client/src/components/sidebar/ApprovalItem.tsx"
      provides: "Premium approval cards with mobile touch targets"
      contains: "min-h-[44px]"
    - path: "client/src/components/sidebar/TaskPipelineView.tsx"
      provides: "Pipeline view with category-colored dots and row hover"
      contains: "whileHover"
  key_links:
    - from: "ApprovalItem.tsx"
      to: "ApprovalsTab.tsx"
      via: "rendered in approvals list"
      pattern: "premium-card"
---

<objective>
Polish the Approvals tab components: approval items and task pipeline view.

Purpose: The Approvals tab is where users make critical decisions (approve/reject autonomous work). Touch targets must be reliable on mobile, and the pipeline view needs to feel like a real status board.
Output: Two components with premium treatment, mobile-compliant action buttons, and category-colored pipeline.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-polish/15-CONTEXT.md
@.planning/phases/15-polish/15-RESEARCH.md

@client/src/components/sidebar/ApprovalItem.tsx
@client/src/components/sidebar/TaskPipelineView.tsx
@client/src/components/sidebar/approvalUtils.ts
@client/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish ApprovalItem — premium card + mobile touch targets + hover emphasis</name>
  <files>client/src/components/sidebar/ApprovalItem.tsx</files>
  <read_first>
    - client/src/components/sidebar/ApprovalItem.tsx
    - client/src/index.css
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Polish ApprovalItem with these specific changes:

1. **Premium card class**: Replace the existing background classes on the outermost container with `premium-card` CSS class. This unifies the card treatment with ActivityFeedItem and other sidebar cards.

2. **Mobile touch targets on approve/reject buttons**: Add `min-h-[44px] lg:min-h-auto` to both the Approve and Reject buttons. These are critical action buttons that must be reliably tappable on mobile.

3. **Hover emphasis on approve button**: Add `whileHover={{ scale: 1.02 }}` on the Approve button using Framer Motion's `motion.button`. This provides subtle visual feedback that the approve action is emphasized over reject.

4. **Entry animation**: Add standard entry on the card container:
   ```
   initial={{ opacity: 0, y: 8 }}
   animate={{ opacity: 1, y: 0 }}
   transition={{ duration: 0.18, ease: 'easeOut' }}
   ```

5. **Badge consistency**: Ensure any risk/status badges use unified pill: `text-[10px] font-semibold px-2 py-0.5 rounded-full`.

6. **Fix undefined CSS variables**: Replace any `var(--hatchin-surface-hover)` with `var(--hatchin-surface-elevated)`.

7. **Spacing**: Snap all padding/margin/gap to 4px grid.

After changes, run typecheck and take Playwright screenshots at desktop and 375px mobile width to verify button sizes.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ApprovalItem.tsx contains `premium-card` class
    - ApprovalItem.tsx contains `min-h-[44px]` on approve and reject buttons
    - ApprovalItem.tsx contains `lg:min-h-auto` for desktop override
    - ApprovalItem.tsx contains `whileHover` on the approve button
    - ApprovalItem.tsx does NOT contain `--hatchin-surface-hover`
    - TypeScript typecheck passes with zero errors
    - Playwright screenshot at 375px shows visibly tappable approve/reject buttons
  </acceptance_criteria>
  <done>Approval items have premium card, 44px mobile touch targets, and approve button emphasis</done>
</task>

<task type="auto">
  <name>Task 2: Polish TaskPipelineView — stage colors + row hover + spacing</name>
  <files>client/src/components/sidebar/TaskPipelineView.tsx</files>
  <read_first>
    - client/src/components/sidebar/TaskPipelineView.tsx
    - client/src/components/sidebar/approvalUtils.ts
    - .planning/phases/15-polish/15-RESEARCH.md
  </read_first>
  <action>
Polish TaskPipelineView with these specific changes:

1. **Stage dot colors using design-system variables**: Replace any Tailwind arbitrary colors on pipeline stage dots with CSS custom property pattern. Map stage colors to the established category palette:
   - Queued: `bg-[var(--hatchin-text-muted)]` (gray)
   - Assigned: `bg-[var(--hatchin-blue)]` (blue)
   - In Progress: `bg-[var(--hatchin-blue)]` (blue, active)
   - Review: `bg-[var(--hatchin-orange)]` (orange/amber)
   - Done: `bg-[var(--hatchin-green)]` (green)

2. **Row hover state**: Wrap each pipeline row in a `motion.div` with:
   ```
   whileHover={{ backgroundColor: 'var(--hatchin-surface-elevated)' }}
   transition={{ duration: 0.15 }}
   className="rounded-lg px-2 py-1 -mx-2"
   ```
   This adds a subtle hover highlight on each task row.

3. **Section header pattern**: If the pipeline has a title/header, apply the unified section header pattern:
   ```
   <div className="flex items-center gap-2 mb-3">
     <span className="text-[11px] font-semibold text-[var(--hatchin-text-muted)] uppercase tracking-wider">
       Pipeline
     </span>
     <div className="flex-1 h-px bg-gradient-to-r from-[var(--hatchin-border-subtle)] to-transparent" />
   </div>
   ```

4. **Spacing**: Snap all padding/margin/gap to 4px grid.

After changes, run typecheck and take a Playwright screenshot of the pipeline view in the Approvals tab.
  </action>
  <verify>
    <automated>cd /Users/shashankrai/Documents/hatching-mvp-5th-march && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - TaskPipelineView.tsx contains `var(--hatchin-green)` or `var(--hatchin-blue)` for stage dot colors (not raw Tailwind colors)
    - TaskPipelineView.tsx contains `whileHover` for row hover state
    - TaskPipelineView.tsx contains `motion.div` or `motion` import from framer-motion
    - TypeScript typecheck passes with zero errors
    - Playwright screenshot shows pipeline with visible stage dots and clear stage labels
  </acceptance_criteria>
  <done>Pipeline view has design-system stage colors, row hover states, and consistent section header</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit --pretty` passes with zero errors
2. Playwright screenshots of: (a) Approval items with premium cards at mobile width, (b) Task pipeline with colored stage dots
3. All CSS variable references point to defined variables
</verification>

<success_criteria>
- Approval items use premium-card with 44px mobile touch targets on action buttons
- Pipeline stage dots use design-system color variables
- Pipeline rows have hover highlight
- All changes pass TypeScript strict mode
</success_criteria>

<output>
After completion, create `.planning/phases/15-polish/15-03-SUMMARY.md`
</output>
