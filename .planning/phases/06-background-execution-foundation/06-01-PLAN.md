---
phase: 06-background-execution-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/autonomy/triggers/autonomyTriggerResolver.ts
  - server/autonomy/config/policies.ts
  - server/autonomy/execution/jobQueue.ts
  - scripts/test-execution-trigger.ts
autonomous: true
requirements: [EXEC-01, EXEC-03]
must_haves:
  truths:
    - "When a user says 'go ahead and work on this' and pending todo tasks exist, the system identifies tasks eligible for autonomous execution"
    - "When the feature flag is off, trigger phrases do not queue any tasks for autonomous execution"
    - "When the daily cost cap is reached, no further tasks are queued for a given project that day"
    - "The job queue accepts task execution requests and persists them durably across server restarts"
    - "A non-trigger phrase like 'hello how are you' does not activate autonomous execution"
  artifacts:
    - path: "server/autonomy/triggers/autonomyTriggerResolver.ts"
      provides: "Pure trigger resolution function"
      exports: ["resolveAutonomyTrigger", "AutonomyTriggerDecision"]
    - path: "server/autonomy/config/policies.ts"
      provides: "backgroundExecution flag and cost cap constant"
      contains: "backgroundExecution"
    - path: "server/autonomy/execution/jobQueue.ts"
      provides: "pg-boss singleton and job queue functions"
      exports: ["getJobQueue", "queueTaskExecution"]
    - path: "scripts/test-execution-trigger.ts"
      provides: "Unit tests for trigger resolver and cost cap"
  key_links:
    - from: "server/autonomy/triggers/autonomyTriggerResolver.ts"
      to: "server/autonomy/config/policies.ts"
      via: "FEATURE_FLAGS.backgroundExecution check"
      pattern: "FEATURE_FLAGS\\.backgroundExecution"
    - from: "server/autonomy/execution/jobQueue.ts"
      to: "server/autonomy/config/policies.ts"
      via: "FEATURE_FLAGS.backgroundExecution guard"
      pattern: "FEATURE_FLAGS\\.backgroundExecution"
---

<objective>
Create the trigger resolver, cost cap policy, and durable job queue that form the foundation for autonomous execution.

Purpose: EXEC-01 requires detecting explicit "go ahead" triggers. EXEC-03 requires per-project daily LLM spend caps. Both must exist before any execution pipeline can run.
Output: Three new modules (autonomyTriggerResolver.ts, jobQueue.ts, policies.ts additions) and a test script validating trigger + cap logic.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-background-execution-foundation/06-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From server/autonomy/config/policies.ts:
```typescript
export interface CognitiveBudgets {
  maxSearches: number;
  maxPages: number;
  maxReviewers: number;
  maxRevisionCycles: number;
  maxDeliberationRounds: number;
  hardResponseTimeoutMs: number;
  singleResponseBudgetMs: number;
  deliberationBudgetMs: number;
  safetyTriggerBudgetMs: number;
}

export const FEATURE_FLAGS = {
  peerPolicing: ...,
  akl: ...,
  taskGraph: ...,
  toolRouter: ...,
  autonomyDashboard: ...,
};

export const BUDGETS: CognitiveBudgets = { ... };
```

From shared/schema.ts (tasks table):
```typescript
export const tasks = pgTable("tasks", {
  id: varchar("id").primaryKey().default(sql`gen_random_uuid()`),
  status: text("status").$type<"todo" | "in_progress" | "completed" | "blocked">().default("todo"),
  assignee: text("assignee"), // Agent name/role string, NOT agent UUID
  projectId: varchar("project_id").references(() => projects.id).notNull(),
  // ...
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AutonomyTriggerResolver and policies.ts additions with tests</name>
  <files>
    server/autonomy/triggers/autonomyTriggerResolver.ts,
    server/autonomy/config/policies.ts,
    scripts/test-execution-trigger.ts
  </files>
  <read_first>
    server/autonomy/config/policies.ts,
    shared/schema.ts
  </read_first>
  <behavior>
    - resolveAutonomyTrigger({userMessage: "go ahead and work on this", pendingTasks: [{id:"t1", status:"todo"}], autonomyEnabled: true, lastUserActivityAt: new Date()}) returns {shouldExecute: true, reason: "explicit", tasksToExecute: ["t1"]}
    - resolveAutonomyTrigger({userMessage: "go ahead", pendingTasks: [{id:"t1", status:"completed"}], autonomyEnabled: true, lastUserActivityAt: new Date()}) returns {shouldExecute: false} (no todo tasks)
    - resolveAutonomyTrigger({userMessage: "go ahead", pendingTasks: [{id:"t1", status:"todo"}], autonomyEnabled: false, lastUserActivityAt: new Date()}) returns {shouldExecute: false, reason: "none"}
    - resolveAutonomyTrigger({userMessage: "hello how are you", pendingTasks: [{id:"t1", status:"todo"}], autonomyEnabled: true, lastUserActivityAt: new Date()}) returns {shouldExecute: false} (no trigger phrase)
    - resolveAutonomyTrigger with no userMessage and no lastUserActivityAt returns {shouldExecute: false}
    - Trigger phrases include: "go ahead", "work on this", "take it from here", "you handle it", "start working", "begin execution", "handle this autonomously", "go work on"
  </behavior>
  <action>
    **Step 1: Write test script** `scripts/test-execution-trigger.ts`:
    - Import `resolveAutonomyTrigger` from `../server/autonomy/triggers/autonomyTriggerResolver.js`
    - Write 6 test cases per the behavior block above
    - Use `process.exit(1)` on failure, `process.exit(0)` on success
    - Follow existing script pattern from `scripts/test-*.ts` files

    **Step 2: Create** `server/autonomy/triggers/autonomyTriggerResolver.ts`:
    - Export `AutonomyTriggerDecision` interface: `{shouldExecute: boolean, reason: 'explicit' | 'inactivity' | 'none', tasksToExecute: string[]}`
    - Export `resolveAutonomyTrigger(input: {userMessage?: string, lastUserActivityAt: Date | null, pendingTasks: Array<{id: string, status: string}>, autonomyEnabled: boolean}): AutonomyTriggerDecision`
    - TRIGGER_PHRASES array (module-level const, not exported): `['go ahead', 'work on this', 'take it from here', 'you handle it', 'start working', 'begin execution', 'handle this autonomously', 'go work on']`
    - If `!input.autonomyEnabled`, return `{shouldExecute: false, reason: 'none', tasksToExecute: []}`
    - Check `(input.userMessage ?? '').toLowerCase()` against TRIGGER_PHRASES via `.some(phrase => lower.includes(phrase))`
    - If explicit match found, filter `pendingTasks` to those with `status === 'todo'`, return their IDs
    - If no match, return `{shouldExecute: false, reason: 'none', tasksToExecute: []}`
    - This is a PURE function: no I/O, no imports of storage or LLM

    **Step 3: Modify** `server/autonomy/config/policies.ts`:
    - Add to `CognitiveBudgets` interface: `maxConcurrentAutonomousTasks: number` and `maxBackgroundLlmCallsPerProjectPerDay: number`
    - Add to `BUDGETS` object: `maxConcurrentAutonomousTasks: Number(process.env.MAX_CONCURRENT_AUTONOMOUS_TASKS ?? 3)` and `maxBackgroundLlmCallsPerProjectPerDay: Number(process.env.MAX_BACKGROUND_LLM_CALLS_PER_PROJECT_PER_DAY ?? 5)`
    - Add to `FEATURE_FLAGS` object: `backgroundExecution: (process.env.BACKGROUND_AUTONOMY_ENABLED ?? 'false').toLowerCase() === 'true'`
    - Do NOT remove or modify any existing properties

    **Step 4: Run tests** to confirm GREEN.
  </action>
  <verify>
    <automated>npx tsx scripts/test-execution-trigger.ts</automated>
  </verify>
  <acceptance_criteria>
    - server/autonomy/triggers/autonomyTriggerResolver.ts exports `resolveAutonomyTrigger` and `AutonomyTriggerDecision`
    - server/autonomy/config/policies.ts contains `backgroundExecution:` in FEATURE_FLAGS
    - server/autonomy/config/policies.ts contains `maxBackgroundLlmCallsPerProjectPerDay:` in BUDGETS
    - server/autonomy/config/policies.ts contains `maxConcurrentAutonomousTasks:` in BUDGETS
    - scripts/test-execution-trigger.ts exits 0
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>resolveAutonomyTrigger correctly detects trigger phrases, respects autonomyEnabled flag, filters to todo tasks only. policies.ts has backgroundExecution flag and cost cap. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create pg-boss job queue module</name>
  <files>
    server/autonomy/execution/jobQueue.ts
  </files>
  <read_first>
    server/autonomy/config/policies.ts,
    server/db.ts,
    package.json
  </read_first>
  <action>
    **Step 1: Install pg-boss:**
    ```bash
    npm install pg-boss
    ```

    **Step 2: Create** `server/autonomy/execution/jobQueue.ts`:
    - Import `PgBoss` from `'pg-boss'`
    - Import `FEATURE_FLAGS` from `'../config/policies.js'`
    - Module-level `let _boss: PgBoss | null = null`
    - Export `async function getJobQueue(): Promise<PgBoss | null>` -- returns null if `!FEATURE_FLAGS.backgroundExecution`; creates singleton `new PgBoss(process.env.DATABASE_URL!)` and calls `await _boss.start()` on first call; returns cached instance on subsequent calls
    - Export `async function queueTaskExecution(data: {taskId: string, projectId: string, agentId: string}): Promise<string | null>` -- gets boss via `getJobQueue()`, returns null if no boss; calls `boss.send('autonomous_task_execution', data, {retryLimit: 3, retryDelay: 30, expireInMinutes: 30, singletonKey: data.taskId})`
    - Export `async function stopJobQueue(): Promise<void>` -- calls `_boss?.stop()` and sets `_boss = null`
    - NOTE: pg-boss uses the standard `pg` driver internally against `DATABASE_URL`. This is intentional -- the Neon serverless driver continues to power Drizzle ORM. pg-boss manages its own `pgboss.*` schema on first `.start()`.
    - Do NOT register any `.work()` handler in this file -- that belongs in the execution pipeline (Plan 03, via `startTaskWorker`).

    **Step 3: Verify** `npx tsc --noEmit` passes. The job queue cannot be integration-tested without a database, so typecheck is the gate here.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - server/autonomy/execution/jobQueue.ts exists and exports `getJobQueue`, `queueTaskExecution`, `stopJobQueue`
    - package.json contains `"pg-boss"` in dependencies
    - `npx tsc --noEmit` passes
    - jobQueue.ts imports from `'pg-boss'` and `'../config/policies.js'`
    - jobQueue.ts checks `FEATURE_FLAGS.backgroundExecution` before initializing
  </acceptance_criteria>
  <done>pg-boss job queue module created with singleton pattern, feature flag guard, and deduplication via singletonKey. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsx scripts/test-execution-trigger.ts` exits 0
- `npx tsc --noEmit` passes
- `npm install` succeeds with pg-boss added
</verification>

<success_criteria>
- AutonomyTriggerResolver correctly identifies 8 trigger phrases and returns task IDs
- policies.ts has backgroundExecution flag (default false) and daily cost cap (default 5)
- pg-boss job queue module exists with singleton init, feature flag guard, and deduplication
- All new code passes TypeScript strict mode
</success_criteria>

<output>
After completion, create `.planning/phases/06-background-execution-foundation/06-01-SUMMARY.md`
</output>
