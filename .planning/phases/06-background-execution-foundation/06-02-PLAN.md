---
phase: 06-background-execution-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/ai/safety.ts
  - scripts/gate-safety.ts
autonomous: true
requirements: [SAFE-02]
must_haves:
  truths:
    - "evaluateSafetyScore with executionContext 'autonomous_task' returns higher executionRisk than same input with default context"
    - "evaluateSafetyScore without executionContext parameter behaves identically to current behavior (backward compatible)"
    - "AUTONOMOUS_SAFETY_THRESHOLDS.clarificationRequiredRisk is 0.60 (stricter than chat 0.70)"
  artifacts:
    - path: "server/ai/safety.ts"
      provides: "Extended safety scoring with autonomous context"
      exports: ["evaluateSafetyScore", "AUTONOMOUS_SAFETY_THRESHOLDS", "needsClarification"]
      contains: "executionContext"
  key_links:
    - from: "server/ai/safety.ts"
      to: "server/ai/autonomyTypes.ts"
      via: "SafetyScore type import"
      pattern: "import.*SafetyScore.*autonomyTypes"
---

<objective>
Extend safety.ts to support autonomous execution context with stricter risk thresholds.

Purpose: SAFE-02 requires safety scoring adapted for autonomous context where no user message is present. Autonomous tasks get a +0.10 executionRisk baseline and a stricter 0.60 clarification threshold.
Output: Modified safety.ts with backward-compatible executionContext parameter and new AUTONOMOUS_SAFETY_THRESHOLDS export.
</objective>

<execution_context>
@/Users/shashankrai/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shashankrai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-background-execution-foundation/06-RESEARCH.md

<interfaces>
From server/ai/safety.ts (current signature):
```typescript
export function evaluateSafetyScore(input: {
  userMessage: string;
  draftResponse?: string;
  conversationMode: "project" | "team" | "agent";
  projectName?: string;
}): SafetyScore;
```

From server/ai/autonomyTypes.ts:
```typescript
export interface SafetyScore {
  hallucinationRisk: number;
  scopeRisk: number;
  executionRisk: number;
  confidence: number;
  reasons: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend evaluateSafetyScore with executionContext parameter</name>
  <files>
    server/ai/safety.ts,
    scripts/gate-safety.ts
  </files>
  <read_first>
    server/ai/safety.ts,
    server/ai/autonomyTypes.ts,
    scripts/gate-safety.ts
  </read_first>
  <behavior>
    - evaluateSafetyScore({userMessage: "build auth module", conversationMode: "project"}) returns executionRisk ~0.10 (no autonomous boost)
    - evaluateSafetyScore({userMessage: "build auth module", conversationMode: "project", executionContext: "autonomous_task"}) returns executionRisk ~0.20 (+0.10 boost)
    - evaluateSafetyScore({userMessage: "build auth module", conversationMode: "project", executionContext: "chat"}) returns same as no executionContext (backward compatible)
    - evaluateSafetyScore with no executionContext parameter works exactly as before (all existing tests must still pass)
    - AUTONOMOUS_SAFETY_THRESHOLDS.clarificationRequiredRisk equals 0.60
    - AUTONOMOUS_SAFETY_THRESHOLDS.peerReviewTrigger equals 0.35
  </behavior>
  <action>
    **Step 1: Read and understand** existing `scripts/gate-safety.ts` to understand the test pattern.

    **Step 2: Add test cases** to `scripts/gate-safety.ts`:
    - Add a test section "Autonomous context safety scoring" with these cases:
      - Call `evaluateSafetyScore` with `executionContext: 'autonomous_task'` and a benign `userMessage: "write a project plan"`. Assert `executionRisk >= 0.20` (base 0.10 + autonomous boost 0.10).
      - Call with same input but `executionContext: 'chat'` (or omit it). Assert `executionRisk < 0.20` (no boost).
      - Call with `executionContext: 'autonomous_task'` and a risky `userMessage: "delete production deploy"`. Assert `executionRisk >= 0.35` (risky keyword + autonomous boost).
    - Add assertion: `AUTONOMOUS_SAFETY_THRESHOLDS.clarificationRequiredRisk === 0.60`
    - Add assertion: `AUTONOMOUS_SAFETY_THRESHOLDS.peerReviewTrigger === 0.35`
    - Run tests to confirm FAIL (RED phase).

    **Step 3: Modify** `server/ai/safety.ts`:
    - Add optional `executionContext?: 'chat' | 'autonomous_task'` to the input parameter type of `evaluateSafetyScore`. Default behavior (when omitted or `'chat'`) is unchanged.
    - After the existing `executionRisk` calculation and BEFORE the final `Math.min(1, ...)` clamping, add:
      ```typescript
      if (input.executionContext === 'autonomous_task') {
        executionRisk = Math.min(1, executionRisk + 0.10);
        reasons.push('autonomous_context_risk_boost');
      }
      ```
    - Add new exported const AFTER the existing `SAFETY_THRESHOLDS`:
      ```typescript
      export const AUTONOMOUS_SAFETY_THRESHOLDS = {
        peerReviewTrigger: 0.35,
        clarificationRequiredRisk: 0.60,
      } as const;
      ```
    - Do NOT modify the `needsClarification` function or `buildClarificationIntervention` — those remain chat-only. The autonomous pipeline will use `AUTONOMOUS_SAFETY_THRESHOLDS` directly for its own threshold checks.

    **Step 4: Run tests** to confirm GREEN.

    **Step 5: Run existing safety gate** `npm run gate:safety` to confirm no regression.
  </action>
  <verify>
    <automated>npm run gate:safety</automated>
  </verify>
  <acceptance_criteria>
    - server/ai/safety.ts contains `executionContext?: 'chat' | 'autonomous_task'` in evaluateSafetyScore input type
    - server/ai/safety.ts contains `autonomous_context_risk_boost` string in a reasons.push call
    - server/ai/safety.ts exports `AUTONOMOUS_SAFETY_THRESHOLDS` with `clarificationRequiredRisk: 0.60`
    - `npm run gate:safety` exits 0
    - `npx tsc --noEmit` passes
    - Calling evaluateSafetyScore without executionContext produces identical results to before (backward compatible)
  </acceptance_criteria>
  <done>safety.ts accepts executionContext parameter; autonomous_task context adds +0.10 to executionRisk; AUTONOMOUS_SAFETY_THRESHOLDS exported with 0.60 clarification threshold; all existing safety tests pass.</done>
</task>

</tasks>

<verification>
- `npm run gate:safety` exits 0
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- evaluateSafetyScore is backward compatible (no existing callers break)
- Autonomous context adds +0.10 executionRisk baseline
- AUTONOMOUS_SAFETY_THRESHOLDS exported with stricter 0.60 threshold
- All safety gate tests green
</success_criteria>

<output>
After completion, create `.planning/phases/06-background-execution-foundation/06-02-SUMMARY.md`
</output>
